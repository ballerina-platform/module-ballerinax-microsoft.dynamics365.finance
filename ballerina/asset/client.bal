// AUTO-GENERATED FILE. DO NOT MODIFY.
// This file is auto-generated by the Ballerina OpenAPI tool.

import ballerina/data.jsondata;
import ballerina/http;

public isolated client class Client {
    final http:Client clientEp;
    # Gets invoked to initialize the `connector`.
    #
    # + config - The configurations to be used when initializing the `connector` 
    # + serviceUrl - URL of the target service 
    # + return - An error if connector initialization failed 
    public isolated function init(ConnectionConfig config, string serviceUrl = "https://your-org.operations.dynamics.com/data") returns error? {
        http:ClientConfiguration httpClientConfig = {auth: config.auth, httpVersion: config.httpVersion, http1Settings: config.http1Settings, http2Settings: config.http2Settings, timeout: config.timeout, forwarded: config.forwarded, followRedirects: config.followRedirects, poolConfig: config.poolConfig, cache: config.cache, compression: config.compression, circuitBreaker: config.circuitBreaker, retryConfig: config.retryConfig, cookieConfig: config.cookieConfig, responseLimits: config.responseLimits, secureSocket: config.secureSocket, proxy: config.proxy, socketConfig: config.socketConfig, validation: config.validation, laxDataBinding: config.laxDataBinding};
        self.clientEp = check new (serviceUrl, httpClientConfig);
    }

    # List AssetLendings
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of AssetLending 
    remote isolated function listAssetLendings(map<string|string[]> headers = {}, *ListAssetLendingsQueries queries) returns AssetLendingsCollection|error {
        string resourcePath = string `/AssetLendings`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create AssetLending
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AssetLending created 
    remote isolated function createAssetLendings(AssetLending payload, map<string|string[]> headers = {}) returns AssetLending|error {
        string resourcePath = string `/AssetLendings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get AssetLending by key
    #
    # + dataAreaId - The company data area identifier 
    # + fAInventoryNumber - The f a inventory number key field 
    # + dateOfLease - The date of lease key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - AssetLending 
    remote isolated function getAssetLendings(string dataAreaId, string fAInventoryNumber, string dateOfLease, map<string|string[]> headers = {}, *GetAssetLendingsQueries queries) returns AssetLending|error {
        string resourcePath = string `/AssetLendings(dataAreaId=${getEncodedUri(dataAreaId)},FAInventoryNumber=${getEncodedUri(fAInventoryNumber)},DateOfLease=${getEncodedUri(dateOfLease)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete AssetLending
    #
    # + dataAreaId - The company data area identifier 
    # + fAInventoryNumber - The f a inventory number key field 
    # + dateOfLease - The date of lease key field 
    # + headers - Headers to be sent with the request 
    # + return - AssetLending deleted 
    remote isolated function deleteAssetLendings(string dataAreaId, string fAInventoryNumber, string dateOfLease, DeleteAssetLendingsHeaders headers = {}) returns error? {
        string resourcePath = string `/AssetLendings(dataAreaId=${getEncodedUri(dataAreaId)},FAInventoryNumber=${getEncodedUri(fAInventoryNumber)},DateOfLease=${getEncodedUri(dateOfLease)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update AssetLending
    #
    # + dataAreaId - The company data area identifier 
    # + fAInventoryNumber - The f a inventory number key field 
    # + dateOfLease - The date of lease key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AssetLending updated 
    remote isolated function updateAssetLendings(string dataAreaId, string fAInventoryNumber, string dateOfLease, AssetLending payload, UpdateAssetLendingsHeaders headers = {}) returns AssetLending|error {
        string resourcePath = string `/AssetLendings(dataAreaId=${getEncodedUri(dataAreaId)},FAInventoryNumber=${getEncodedUri(fAInventoryNumber)},DateOfLease=${getEncodedUri(dateOfLease)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List AssetSortings
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of AssetSorting 
    remote isolated function listAssetSortings(map<string|string[]> headers = {}, *ListAssetSortingsQueries queries) returns AssetSortingsCollection|error {
        string resourcePath = string `/AssetSortings`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create AssetSorting
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AssetSorting created 
    remote isolated function createAssetSortings(AssetSorting payload, map<string|string[]> headers = {}) returns AssetSorting|error {
        string resourcePath = string `/AssetSortings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get AssetSorting by key
    #
    # + dataAreaId - The company data area identifier 
    # + sorting - The sorting key field 
    # + identification - The identification key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - AssetSorting 
    remote isolated function getAssetSortings(string dataAreaId, string sorting, string identification, map<string|string[]> headers = {}, *GetAssetSortingsQueries queries) returns AssetSorting|error {
        string resourcePath = string `/AssetSortings(dataAreaId=${getEncodedUri(dataAreaId)},Sorting=${getEncodedUri(sorting)},Identification=${getEncodedUri(identification)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete AssetSorting
    #
    # + dataAreaId - The company data area identifier 
    # + sorting - The sorting key field 
    # + identification - The identification key field 
    # + headers - Headers to be sent with the request 
    # + return - AssetSorting deleted 
    remote isolated function deleteAssetSortings(string dataAreaId, string sorting, string identification, DeleteAssetSortingsHeaders headers = {}) returns error? {
        string resourcePath = string `/AssetSortings(dataAreaId=${getEncodedUri(dataAreaId)},Sorting=${getEncodedUri(sorting)},Identification=${getEncodedUri(identification)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update AssetSorting
    #
    # + dataAreaId - The company data area identifier 
    # + sorting - The sorting key field 
    # + identification - The identification key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AssetSorting updated 
    remote isolated function updateAssetSortings(string dataAreaId, string sorting, string identification, AssetSorting payload, UpdateAssetSortingsHeaders headers = {}) returns AssetSorting|error {
        string resourcePath = string `/AssetSortings(dataAreaId=${getEncodedUri(dataAreaId)},Sorting=${getEncodedUri(sorting)},Identification=${getEncodedUri(identification)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List BankAccounts
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of BankAccount 
    remote isolated function listBankAccounts(map<string|string[]> headers = {}, *ListBankAccountsQueries queries) returns BankAccountsCollection|error {
        string resourcePath = string `/BankAccounts`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create BankAccount
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - BankAccount created 
    remote isolated function createBankAccounts(BankAccount payload, map<string|string[]> headers = {}) returns BankAccount|error {
        string resourcePath = string `/BankAccounts`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get BankAccount by key
    #
    # + dataAreaId - The company data area identifier 
    # + bankAccountId - The bank account id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - BankAccount 
    remote isolated function getBankAccounts(string dataAreaId, string bankAccountId, map<string|string[]> headers = {}, *GetBankAccountsQueries queries) returns BankAccount|error {
        string resourcePath = string `/BankAccounts(dataAreaId=${getEncodedUri(dataAreaId)},BankAccountId=${getEncodedUri(bankAccountId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete BankAccount
    #
    # + dataAreaId - The company data area identifier 
    # + bankAccountId - The bank account id key field 
    # + headers - Headers to be sent with the request 
    # + return - BankAccount deleted 
    remote isolated function deleteBankAccounts(string dataAreaId, string bankAccountId, DeleteBankAccountsHeaders headers = {}) returns error? {
        string resourcePath = string `/BankAccounts(dataAreaId=${getEncodedUri(dataAreaId)},BankAccountId=${getEncodedUri(bankAccountId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update BankAccount
    #
    # + dataAreaId - The company data area identifier 
    # + bankAccountId - The bank account id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - BankAccount updated 
    remote isolated function updateBankAccounts(string dataAreaId, string bankAccountId, BankAccount payload, UpdateBankAccountsHeaders headers = {}) returns BankAccount|error {
        string resourcePath = string `/BankAccounts(dataAreaId=${getEncodedUri(dataAreaId)},BankAccountId=${getEncodedUri(bankAccountId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List BankGroups
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of BankGroup 
    remote isolated function listBankGroups(map<string|string[]> headers = {}, *ListBankGroupsQueries queries) returns BankGroupsCollection|error {
        string resourcePath = string `/BankGroups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create BankGroup
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - BankGroup created 
    remote isolated function createBankGroups(BankGroup payload, map<string|string[]> headers = {}) returns BankGroup|error {
        string resourcePath = string `/BankGroups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get BankGroup by key
    #
    # + dataAreaId - The company data area identifier 
    # + bankGroupId - The bank group id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - BankGroup 
    remote isolated function getBankGroups(string dataAreaId, string bankGroupId, map<string|string[]> headers = {}, *GetBankGroupsQueries queries) returns BankGroup|error {
        string resourcePath = string `/BankGroups(dataAreaId=${getEncodedUri(dataAreaId)},BankGroupId=${getEncodedUri(bankGroupId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete BankGroup
    #
    # + dataAreaId - The company data area identifier 
    # + bankGroupId - The bank group id key field 
    # + headers - Headers to be sent with the request 
    # + return - BankGroup deleted 
    remote isolated function deleteBankGroups(string dataAreaId, string bankGroupId, DeleteBankGroupsHeaders headers = {}) returns error? {
        string resourcePath = string `/BankGroups(dataAreaId=${getEncodedUri(dataAreaId)},BankGroupId=${getEncodedUri(bankGroupId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update BankGroup
    #
    # + dataAreaId - The company data area identifier 
    # + bankGroupId - The bank group id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - BankGroup updated 
    remote isolated function updateBankGroups(string dataAreaId, string bankGroupId, BankGroup payload, UpdateBankGroupsHeaders headers = {}) returns BankGroup|error {
        string resourcePath = string `/BankGroups(dataAreaId=${getEncodedUri(dataAreaId)},BankGroupId=${getEncodedUri(bankGroupId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List CDSProjects
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of CDSProject 
    remote isolated function listCDSProjects(map<string|string[]> headers = {}, *ListCDSProjectsQueries queries) returns CDSProjectsCollection|error {
        string resourcePath = string `/CDSProjects`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create CDSProject
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CDSProject created 
    remote isolated function createCDSProjects(CDSProject payload, map<string|string[]> headers = {}) returns CDSProject|error {
        string resourcePath = string `/CDSProjects`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get CDSProject by key
    #
    # + dataAreaId - The company data area identifier 
    # + projectID - The project id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CDSProject 
    remote isolated function getCDSProjects(string dataAreaId, string projectID, map<string|string[]> headers = {}, *GetCDSProjectsQueries queries) returns CDSProject|error {
        string resourcePath = string `/CDSProjects(dataAreaId=${getEncodedUri(dataAreaId)},ProjectID=${getEncodedUri(projectID)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete CDSProject
    #
    # + dataAreaId - The company data area identifier 
    # + projectID - The project id key field 
    # + headers - Headers to be sent with the request 
    # + return - CDSProject deleted 
    remote isolated function deleteCDSProjects(string dataAreaId, string projectID, DeleteCDSProjectsHeaders headers = {}) returns error? {
        string resourcePath = string `/CDSProjects(dataAreaId=${getEncodedUri(dataAreaId)},ProjectID=${getEncodedUri(projectID)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update CDSProject
    #
    # + dataAreaId - The company data area identifier 
    # + projectID - The project id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CDSProject updated 
    remote isolated function updateCDSProjects(string dataAreaId, string projectID, CDSProject payload, UpdateCDSProjectsHeaders headers = {}) returns CDSProject|error {
        string resourcePath = string `/CDSProjects(dataAreaId=${getEncodedUri(dataAreaId)},ProjectID=${getEncodedUri(projectID)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List CashAccounts
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of CashAccounts 
    remote isolated function listCashAccounts(map<string|string[]> headers = {}, *ListCashAccountsQueries queries) returns CashAccountsCollection|error {
        string resourcePath = string `/CashAccounts`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create CashAccounts
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CashAccounts created 
    remote isolated function createCashAccounts(CashAccounts payload, map<string|string[]> headers = {}) returns CashAccounts|error {
        string resourcePath = string `/CashAccounts`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get CashAccounts by key
    #
    # + dataAreaId - The company data area identifier 
    # + cash - The cash key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CashAccounts 
    remote isolated function getCashAccounts(string dataAreaId, string cash, map<string|string[]> headers = {}, *GetCashAccountsQueries queries) returns CashAccounts|error {
        string resourcePath = string `/CashAccounts(dataAreaId=${getEncodedUri(dataAreaId)},Cash=${getEncodedUri(cash)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete CashAccounts
    #
    # + dataAreaId - The company data area identifier 
    # + cash - The cash key field 
    # + headers - Headers to be sent with the request 
    # + return - CashAccounts deleted 
    remote isolated function deleteCashAccounts(string dataAreaId, string cash, DeleteCashAccountsHeaders headers = {}) returns error? {
        string resourcePath = string `/CashAccounts(dataAreaId=${getEncodedUri(dataAreaId)},Cash=${getEncodedUri(cash)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update CashAccounts
    #
    # + dataAreaId - The company data area identifier 
    # + cash - The cash key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CashAccounts updated 
    remote isolated function updateCashAccounts(string dataAreaId, string cash, CashAccounts payload, UpdateCashAccountsHeaders headers = {}) returns CashAccounts|error {
        string resourcePath = string `/CashAccounts(dataAreaId=${getEncodedUri(dataAreaId)},Cash=${getEncodedUri(cash)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List CashBalances
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of CashBalances 
    remote isolated function listCashBalances(map<string|string[]> headers = {}, *ListCashBalancesQueries queries) returns CashBalancesCollection|error {
        string resourcePath = string `/CashBalances`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create CashBalances
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CashBalances created 
    remote isolated function createCashBalances(CashBalances payload, map<string|string[]> headers = {}) returns CashBalances|error {
        string resourcePath = string `/CashBalances`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get CashBalances by key
    #
    # + dataAreaId - The company data area identifier 
    # + cash - The cash key field 
    # + currency - The currency key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CashBalances 
    remote isolated function getCashBalances(string dataAreaId, string cash, string currency, map<string|string[]> headers = {}, *GetCashBalancesQueries queries) returns CashBalances|error {
        string resourcePath = string `/CashBalances(dataAreaId=${getEncodedUri(dataAreaId)},Cash=${getEncodedUri(cash)},Currency=${getEncodedUri(currency)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete CashBalances
    #
    # + dataAreaId - The company data area identifier 
    # + cash - The cash key field 
    # + currency - The currency key field 
    # + headers - Headers to be sent with the request 
    # + return - CashBalances deleted 
    remote isolated function deleteCashBalances(string dataAreaId, string cash, string currency, DeleteCashBalancesHeaders headers = {}) returns error? {
        string resourcePath = string `/CashBalances(dataAreaId=${getEncodedUri(dataAreaId)},Cash=${getEncodedUri(cash)},Currency=${getEncodedUri(currency)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update CashBalances
    #
    # + dataAreaId - The company data area identifier 
    # + cash - The cash key field 
    # + currency - The currency key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CashBalances updated 
    remote isolated function updateCashBalances(string dataAreaId, string cash, string currency, CashBalances payload, UpdateCashBalancesHeaders headers = {}) returns CashBalances|error {
        string resourcePath = string `/CashBalances(dataAreaId=${getEncodedUri(dataAreaId)},Cash=${getEncodedUri(cash)},Currency=${getEncodedUri(currency)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List CashDiscounts
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of CashDiscount 
    remote isolated function listCashDiscounts(map<string|string[]> headers = {}, *ListCashDiscountsQueries queries) returns CashDiscountsCollection|error {
        string resourcePath = string `/CashDiscounts`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create CashDiscount
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CashDiscount created 
    remote isolated function createCashDiscounts(CashDiscount payload, map<string|string[]> headers = {}) returns CashDiscount|error {
        string resourcePath = string `/CashDiscounts`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get CashDiscount by key
    #
    # + dataAreaId - The company data area identifier 
    # + cashDiscountCode - The cash discount code key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CashDiscount 
    remote isolated function getCashDiscounts(string dataAreaId, string cashDiscountCode, map<string|string[]> headers = {}, *GetCashDiscountsQueries queries) returns CashDiscount|error {
        string resourcePath = string `/CashDiscounts(dataAreaId=${getEncodedUri(dataAreaId)},CashDiscountCode=${getEncodedUri(cashDiscountCode)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete CashDiscount
    #
    # + dataAreaId - The company data area identifier 
    # + cashDiscountCode - The cash discount code key field 
    # + headers - Headers to be sent with the request 
    # + return - CashDiscount deleted 
    remote isolated function deleteCashDiscounts(string dataAreaId, string cashDiscountCode, DeleteCashDiscountsHeaders headers = {}) returns error? {
        string resourcePath = string `/CashDiscounts(dataAreaId=${getEncodedUri(dataAreaId)},CashDiscountCode=${getEncodedUri(cashDiscountCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update CashDiscount
    #
    # + dataAreaId - The company data area identifier 
    # + cashDiscountCode - The cash discount code key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CashDiscount updated 
    remote isolated function updateCashDiscounts(string dataAreaId, string cashDiscountCode, CashDiscount payload, UpdateCashDiscountsHeaders headers = {}) returns CashDiscount|error {
        string resourcePath = string `/CashDiscounts(dataAreaId=${getEncodedUri(dataAreaId)},CashDiscountCode=${getEncodedUri(cashDiscountCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List CashLedgers
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of CashLedger 
    remote isolated function listCashLedgers(map<string|string[]> headers = {}, *ListCashLedgersQueries queries) returns CashLedgersCollection|error {
        string resourcePath = string `/CashLedgers`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create CashLedger
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CashLedger created 
    remote isolated function createCashLedgers(CashLedger payload, map<string|string[]> headers = {}) returns CashLedger|error {
        string resourcePath = string `/CashLedgers`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get CashLedger by key
    #
    # + dataAreaId - The company data area identifier 
    # + cashPosting - The cash posting key field 
    # + mainAccountIdDisplayValue - The main account id display value key field 
    # + cash - The cash key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CashLedger 
    remote isolated function getCashLedgers(string dataAreaId, string cashPosting, string mainAccountIdDisplayValue, string cash, map<string|string[]> headers = {}, *GetCashLedgersQueries queries) returns CashLedger|error {
        string resourcePath = string `/CashLedgers(dataAreaId=${getEncodedUri(dataAreaId)},CashPosting=${getEncodedUri(cashPosting)},MainAccountIdDisplayValue=${getEncodedUri(mainAccountIdDisplayValue)},Cash=${getEncodedUri(cash)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete CashLedger
    #
    # + dataAreaId - The company data area identifier 
    # + cashPosting - The cash posting key field 
    # + mainAccountIdDisplayValue - The main account id display value key field 
    # + cash - The cash key field 
    # + headers - Headers to be sent with the request 
    # + return - CashLedger deleted 
    remote isolated function deleteCashLedgers(string dataAreaId, string cashPosting, string mainAccountIdDisplayValue, string cash, DeleteCashLedgersHeaders headers = {}) returns error? {
        string resourcePath = string `/CashLedgers(dataAreaId=${getEncodedUri(dataAreaId)},CashPosting=${getEncodedUri(cashPosting)},MainAccountIdDisplayValue=${getEncodedUri(mainAccountIdDisplayValue)},Cash=${getEncodedUri(cash)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update CashLedger
    #
    # + dataAreaId - The company data area identifier 
    # + cashPosting - The cash posting key field 
    # + mainAccountIdDisplayValue - The main account id display value key field 
    # + cash - The cash key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CashLedger updated 
    remote isolated function updateCashLedgers(string dataAreaId, string cashPosting, string mainAccountIdDisplayValue, string cash, CashLedger payload, UpdateCashLedgersHeaders headers = {}) returns CashLedger|error {
        string resourcePath = string `/CashLedgers(dataAreaId=${getEncodedUri(dataAreaId)},CashPosting=${getEncodedUri(cashPosting)},MainAccountIdDisplayValue=${getEncodedUri(mainAccountIdDisplayValue)},Cash=${getEncodedUri(cash)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List CashSymbols
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of CashSymbol 
    remote isolated function listCashSymbols(map<string|string[]> headers = {}, *ListCashSymbolsQueries queries) returns CashSymbolsCollection|error {
        string resourcePath = string `/CashSymbols`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create CashSymbol
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CashSymbol created 
    remote isolated function createCashSymbols(CashSymbol payload, map<string|string[]> headers = {}) returns CashSymbol|error {
        string resourcePath = string `/CashSymbols`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get CashSymbol by key
    #
    # + dataAreaId - The company data area identifier 
    # + code - The code key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CashSymbol 
    remote isolated function getCashSymbols(string dataAreaId, string code, map<string|string[]> headers = {}, *GetCashSymbolsQueries queries) returns CashSymbol|error {
        string resourcePath = string `/CashSymbols(dataAreaId=${getEncodedUri(dataAreaId)},Code=${getEncodedUri(code)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete CashSymbol
    #
    # + dataAreaId - The company data area identifier 
    # + code - The code key field 
    # + headers - Headers to be sent with the request 
    # + return - CashSymbol deleted 
    remote isolated function deleteCashSymbols(string dataAreaId, string code, DeleteCashSymbolsHeaders headers = {}) returns error? {
        string resourcePath = string `/CashSymbols(dataAreaId=${getEncodedUri(dataAreaId)},Code=${getEncodedUri(code)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update CashSymbol
    #
    # + dataAreaId - The company data area identifier 
    # + code - The code key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CashSymbol updated 
    remote isolated function updateCashSymbols(string dataAreaId, string code, CashSymbol payload, UpdateCashSymbolsHeaders headers = {}) returns CashSymbol|error {
        string resourcePath = string `/CashSymbols(dataAreaId=${getEncodedUri(dataAreaId)},Code=${getEncodedUri(code)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List CodaTrans
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of CodaTrans 
    remote isolated function listCodaTrans(map<string|string[]> headers = {}, *ListCodaTransQueries queries) returns CodaTransCollection|error {
        string resourcePath = string `/CodaTrans`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create CodaTrans
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CodaTrans created 
    remote isolated function createCodaTrans(CodaTrans payload, map<string|string[]> headers = {}) returns CodaTrans|error {
        string resourcePath = string `/CodaTrans`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get CodaTrans by key
    #
    # + dataAreaId - The company data area identifier 
    # + transactionFamily - The transaction family key field 
    # + 'transaction - The transaction key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CodaTrans 
    remote isolated function getCodaTrans(string dataAreaId, string transactionFamily, string 'transaction, map<string|string[]> headers = {}, *GetCodaTransQueries queries) returns CodaTrans|error {
        string resourcePath = string `/CodaTrans(dataAreaId=${getEncodedUri(dataAreaId)},TransactionFamily=${getEncodedUri(transactionFamily)},Transaction=${getEncodedUri('transaction)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete CodaTrans
    #
    # + dataAreaId - The company data area identifier 
    # + transactionFamily - The transaction family key field 
    # + 'transaction - The transaction key field 
    # + headers - Headers to be sent with the request 
    # + return - CodaTrans deleted 
    remote isolated function deleteCodaTrans(string dataAreaId, string transactionFamily, string 'transaction, DeleteCodaTransHeaders headers = {}) returns error? {
        string resourcePath = string `/CodaTrans(dataAreaId=${getEncodedUri(dataAreaId)},TransactionFamily=${getEncodedUri(transactionFamily)},Transaction=${getEncodedUri('transaction)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update CodaTrans
    #
    # + dataAreaId - The company data area identifier 
    # + transactionFamily - The transaction family key field 
    # + payload - The request body 
    # + 'transaction - The transaction key field 
    # + headers - Headers to be sent with the request 
    # + return - CodaTrans updated 
    remote isolated function updateCodaTrans(string dataAreaId, string transactionFamily, string 'transaction, CodaTrans payload, UpdateCodaTransHeaders headers = {}) returns CodaTrans|error {
        string resourcePath = string `/CodaTrans(dataAreaId=${getEncodedUri(dataAreaId)},TransactionFamily=${getEncodedUri(transactionFamily)},Transaction=${getEncodedUri('transaction)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ExchSetups
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ExchSetup 
    remote isolated function listExchSetups(map<string|string[]> headers = {}, *ListExchSetupsQueries queries) returns ExchSetupsCollection|error {
        string resourcePath = string `/ExchSetups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ExchSetup
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ExchSetup created 
    remote isolated function createExchSetups(ExchSetup payload, map<string|string[]> headers = {}) returns ExchSetup|error {
        string resourcePath = string `/ExchSetups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ExchSetup by key
    #
    # + dataAreaId - The company data area identifier 
    # + exchangeCode - The exchange code key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ExchSetup 
    remote isolated function getExchSetups(string dataAreaId, string exchangeCode, map<string|string[]> headers = {}, *GetExchSetupsQueries queries) returns ExchSetup|error {
        string resourcePath = string `/ExchSetups(dataAreaId=${getEncodedUri(dataAreaId)},ExchangeCode=${getEncodedUri(exchangeCode)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ExchSetup
    #
    # + dataAreaId - The company data area identifier 
    # + exchangeCode - The exchange code key field 
    # + headers - Headers to be sent with the request 
    # + return - ExchSetup deleted 
    remote isolated function deleteExchSetups(string dataAreaId, string exchangeCode, DeleteExchSetupsHeaders headers = {}) returns error? {
        string resourcePath = string `/ExchSetups(dataAreaId=${getEncodedUri(dataAreaId)},ExchangeCode=${getEncodedUri(exchangeCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ExchSetup
    #
    # + dataAreaId - The company data area identifier 
    # + exchangeCode - The exchange code key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ExchSetup updated 
    remote isolated function updateExchSetups(string dataAreaId, string exchangeCode, ExchSetup payload, UpdateExchSetupsHeaders headers = {}) returns ExchSetup|error {
        string resourcePath = string `/ExchSetups(dataAreaId=${getEncodedUri(dataAreaId)},ExchangeCode=${getEncodedUri(exchangeCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ExpenseCodes
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ExpenseCode 
    remote isolated function listExpenseCodes(map<string|string[]> headers = {}, *ListExpenseCodesQueries queries) returns ExpenseCodesCollection|error {
        string resourcePath = string `/ExpenseCodes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ExpenseCode
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ExpenseCode created 
    remote isolated function createExpenseCodes(ExpenseCode payload, map<string|string[]> headers = {}) returns ExpenseCode|error {
        string resourcePath = string `/ExpenseCodes`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ExpenseCode by key
    #
    # + dataAreaId - The company data area identifier 
    # + expenseCode - The expense code key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ExpenseCode 
    remote isolated function getExpenseCodes(string dataAreaId, string expenseCode, map<string|string[]> headers = {}, *GetExpenseCodesQueries queries) returns ExpenseCode|error {
        string resourcePath = string `/ExpenseCodes(dataAreaId=${getEncodedUri(dataAreaId)},ExpenseCode=${getEncodedUri(expenseCode)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ExpenseCode
    #
    # + dataAreaId - The company data area identifier 
    # + expenseCode - The expense code key field 
    # + headers - Headers to be sent with the request 
    # + return - ExpenseCode deleted 
    remote isolated function deleteExpenseCodes(string dataAreaId, string expenseCode, DeleteExpenseCodesHeaders headers = {}) returns error? {
        string resourcePath = string `/ExpenseCodes(dataAreaId=${getEncodedUri(dataAreaId)},ExpenseCode=${getEncodedUri(expenseCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ExpenseCode
    #
    # + dataAreaId - The company data area identifier 
    # + expenseCode - The expense code key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ExpenseCode updated 
    remote isolated function updateExpenseCodes(string dataAreaId, string expenseCode, ExpenseCode payload, UpdateExpenseCodesHeaders headers = {}) returns ExpenseCode|error {
        string resourcePath = string `/ExpenseCodes(dataAreaId=${getEncodedUri(dataAreaId)},ExpenseCode=${getEncodedUri(expenseCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ExpenseParameters
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ExpenseParameters 
    remote isolated function listExpenseParameters(map<string|string[]> headers = {}, *ListExpenseParametersQueries queries) returns ExpenseParametersCollection|error {
        string resourcePath = string `/ExpenseParameters`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ExpenseParameters
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ExpenseParameters created 
    remote isolated function createExpenseParameters(ExpenseParameters payload, map<string|string[]> headers = {}) returns ExpenseParameters|error {
        string resourcePath = string `/ExpenseParameters`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ExpenseParameters by key
    #
    # + dataAreaId - The company data area identifier 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ExpenseParameters 
    remote isolated function getExpenseParameters(string dataAreaId, map<string|string[]> headers = {}, *GetExpenseParametersQueries queries) returns ExpenseParameters|error {
        string resourcePath = string `/ExpenseParameters(${getEncodedUri(dataAreaId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ExpenseParameters
    #
    # + dataAreaId - The company data area identifier 
    # + headers - Headers to be sent with the request 
    # + return - ExpenseParameters deleted 
    remote isolated function deleteExpenseParameters(string dataAreaId, DeleteExpenseParametersHeaders headers = {}) returns error? {
        string resourcePath = string `/ExpenseParameters(${getEncodedUri(dataAreaId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ExpenseParameters
    #
    # + dataAreaId - The company data area identifier 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ExpenseParameters updated 
    remote isolated function updateExpenseParameters(string dataAreaId, ExpenseParameters payload, UpdateExpenseParametersHeaders headers = {}) returns ExpenseParameters|error {
        string resourcePath = string `/ExpenseParameters(${getEncodedUri(dataAreaId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ExpenseRates
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ExpenseRate 
    remote isolated function listExpenseRates(map<string|string[]> headers = {}, *ListExpenseRatesQueries queries) returns ExpenseRatesCollection|error {
        string resourcePath = string `/ExpenseRates`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ExpenseRate
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ExpenseRate created 
    remote isolated function createExpenseRates(ExpenseRate payload, map<string|string[]> headers = {}) returns ExpenseRate|error {
        string resourcePath = string `/ExpenseRates`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ExpenseRate by key
    #
    # + dataAreaId - The company data area identifier 
    # + expense - The expense key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ExpenseRate 
    remote isolated function getExpenseRates(string dataAreaId, string expense, map<string|string[]> headers = {}, *GetExpenseRatesQueries queries) returns ExpenseRate|error {
        string resourcePath = string `/ExpenseRates(dataAreaId=${getEncodedUri(dataAreaId)},Expense=${getEncodedUri(expense)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ExpenseRate
    #
    # + dataAreaId - The company data area identifier 
    # + expense - The expense key field 
    # + headers - Headers to be sent with the request 
    # + return - ExpenseRate deleted 
    remote isolated function deleteExpenseRates(string dataAreaId, string expense, DeleteExpenseRatesHeaders headers = {}) returns error? {
        string resourcePath = string `/ExpenseRates(dataAreaId=${getEncodedUri(dataAreaId)},Expense=${getEncodedUri(expense)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ExpenseRate
    #
    # + dataAreaId - The company data area identifier 
    # + expense - The expense key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ExpenseRate updated 
    remote isolated function updateExpenseRates(string dataAreaId, string expense, ExpenseRate payload, UpdateExpenseRatesHeaders headers = {}) returns ExpenseRate|error {
        string resourcePath = string `/ExpenseRates(dataAreaId=${getEncodedUri(dataAreaId)},Expense=${getEncodedUri(expense)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List Expenses
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of Expenses 
    remote isolated function listExpenses(map<string|string[]> headers = {}, *ListExpensesQueries queries) returns ExpensesCollection|error {
        string resourcePath = string `/Expenses`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create Expenses
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Expenses created 
    remote isolated function createExpenses(Expenses payload, map<string|string[]> headers = {}) returns Expenses|error {
        string resourcePath = string `/Expenses`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get Expenses by key
    #
    # + dataAreaId - The company data area identifier 
    # + expenseTransactionNumber - The expense transaction number key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Expenses 
    remote isolated function getExpenses(string dataAreaId, string expenseTransactionNumber, map<string|string[]> headers = {}, *GetExpensesQueries queries) returns Expenses|error {
        string resourcePath = string `/Expenses(dataAreaId=${getEncodedUri(dataAreaId)},ExpenseTransactionNumber=${getEncodedUri(expenseTransactionNumber)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete Expenses
    #
    # + dataAreaId - The company data area identifier 
    # + expenseTransactionNumber - The expense transaction number key field 
    # + headers - Headers to be sent with the request 
    # + return - Expenses deleted 
    remote isolated function deleteExpenses(string dataAreaId, string expenseTransactionNumber, DeleteExpensesHeaders headers = {}) returns error? {
        string resourcePath = string `/Expenses(dataAreaId=${getEncodedUri(dataAreaId)},ExpenseTransactionNumber=${getEncodedUri(expenseTransactionNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update Expenses
    #
    # + dataAreaId - The company data area identifier 
    # + expenseTransactionNumber - The expense transaction number key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Expenses updated 
    remote isolated function updateExpenses(string dataAreaId, string expenseTransactionNumber, Expenses payload, UpdateExpensesHeaders headers = {}) returns Expenses|error {
        string resourcePath = string `/Expenses(dataAreaId=${getEncodedUri(dataAreaId)},ExpenseTransactionNumber=${getEncodedUri(expenseTransactionNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List FixedAssetValueModels
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of FixedAssetValueModel 
    remote isolated function listFixedAssetValueModels(map<string|string[]> headers = {}, *ListFixedAssetValueModelsQueries queries) returns FixedAssetValueModelsCollection|error {
        string resourcePath = string `/FixedAssetValueModels`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create FixedAssetValueModel
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - FixedAssetValueModel created 
    remote isolated function createFixedAssetValueModels(FixedAssetValueModel payload, map<string|string[]> headers = {}) returns FixedAssetValueModel|error {
        string resourcePath = string `/FixedAssetValueModels`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get FixedAssetValueModel by key
    #
    # + dataAreaId - The company data area identifier 
    # + fixedAssetNumber - The fixed asset number key field 
    # + valueModelId - The value model id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - FixedAssetValueModel 
    remote isolated function getFixedAssetValueModels(string dataAreaId, string fixedAssetNumber, string valueModelId, map<string|string[]> headers = {}, *GetFixedAssetValueModelsQueries queries) returns FixedAssetValueModel|error {
        string resourcePath = string `/FixedAssetValueModels(dataAreaId=${getEncodedUri(dataAreaId)},FixedAssetNumber=${getEncodedUri(fixedAssetNumber)},ValueModelId=${getEncodedUri(valueModelId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete FixedAssetValueModel
    #
    # + dataAreaId - The company data area identifier 
    # + fixedAssetNumber - The fixed asset number key field 
    # + valueModelId - The value model id key field 
    # + headers - Headers to be sent with the request 
    # + return - FixedAssetValueModel deleted 
    remote isolated function deleteFixedAssetValueModels(string dataAreaId, string fixedAssetNumber, string valueModelId, DeleteFixedAssetValueModelsHeaders headers = {}) returns error? {
        string resourcePath = string `/FixedAssetValueModels(dataAreaId=${getEncodedUri(dataAreaId)},FixedAssetNumber=${getEncodedUri(fixedAssetNumber)},ValueModelId=${getEncodedUri(valueModelId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update FixedAssetValueModel
    #
    # + dataAreaId - The company data area identifier 
    # + fixedAssetNumber - The fixed asset number key field 
    # + valueModelId - The value model id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - FixedAssetValueModel updated 
    remote isolated function updateFixedAssetValueModels(string dataAreaId, string fixedAssetNumber, string valueModelId, FixedAssetValueModel payload, UpdateFixedAssetValueModelsHeaders headers = {}) returns FixedAssetValueModel|error {
        string resourcePath = string `/FixedAssetValueModels(dataAreaId=${getEncodedUri(dataAreaId)},FixedAssetNumber=${getEncodedUri(fixedAssetNumber)},ValueModelId=${getEncodedUri(valueModelId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List FixedAssets
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of FixedAsset 
    remote isolated function listFixedAssets(map<string|string[]> headers = {}, *ListFixedAssetsQueries queries) returns FixedAssetsCollection|error {
        string resourcePath = string `/FixedAssets`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create FixedAsset
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - FixedAsset created 
    remote isolated function createFixedAssets(FixedAsset payload, map<string|string[]> headers = {}) returns FixedAsset|error {
        string resourcePath = string `/FixedAssets`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get FixedAsset by key
    #
    # + dataAreaId - The company data area identifier 
    # + fixedAssetNumber - The fixed asset number key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - FixedAsset 
    remote isolated function getFixedAssets(string dataAreaId, string fixedAssetNumber, map<string|string[]> headers = {}, *GetFixedAssetsQueries queries) returns FixedAsset|error {
        string resourcePath = string `/FixedAssets(dataAreaId=${getEncodedUri(dataAreaId)},FixedAssetNumber=${getEncodedUri(fixedAssetNumber)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete FixedAsset
    #
    # + dataAreaId - The company data area identifier 
    # + fixedAssetNumber - The fixed asset number key field 
    # + headers - Headers to be sent with the request 
    # + return - FixedAsset deleted 
    remote isolated function deleteFixedAssets(string dataAreaId, string fixedAssetNumber, DeleteFixedAssetsHeaders headers = {}) returns error? {
        string resourcePath = string `/FixedAssets(dataAreaId=${getEncodedUri(dataAreaId)},FixedAssetNumber=${getEncodedUri(fixedAssetNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update FixedAsset
    #
    # + dataAreaId - The company data area identifier 
    # + fixedAssetNumber - The fixed asset number key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - FixedAsset updated 
    remote isolated function updateFixedAssets(string dataAreaId, string fixedAssetNumber, FixedAsset payload, UpdateFixedAssetsHeaders headers = {}) returns FixedAsset|error {
        string resourcePath = string `/FixedAssets(dataAreaId=${getEncodedUri(dataAreaId)},FixedAssetNumber=${getEncodedUri(fixedAssetNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List FixedAssetsV2
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of FixedAssetV2Entity 
    remote isolated function listFixedAssetsV2(map<string|string[]> headers = {}, *ListFixedAssetsV2Queries queries) returns FixedAssetsV2Collection|error {
        string resourcePath = string `/FixedAssetsV2`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create FixedAssetV2Entity
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - FixedAssetV2Entity created 
    remote isolated function createFixedAssetsV2(FixedAssetV2Entity payload, map<string|string[]> headers = {}) returns FixedAssetV2Entity|error {
        string resourcePath = string `/FixedAssetsV2`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get FixedAssetV2Entity by key
    #
    # + dataAreaId - The company data area identifier 
    # + fixedAssetNumber - The fixed asset number key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - FixedAssetV2Entity 
    remote isolated function getFixedAssetsV2(string dataAreaId, string fixedAssetNumber, map<string|string[]> headers = {}, *GetFixedAssetsV2Queries queries) returns FixedAssetV2Entity|error {
        string resourcePath = string `/FixedAssetsV2(dataAreaId=${getEncodedUri(dataAreaId)},FixedAssetNumber=${getEncodedUri(fixedAssetNumber)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete FixedAssetV2Entity
    #
    # + dataAreaId - The company data area identifier 
    # + fixedAssetNumber - The fixed asset number key field 
    # + headers - Headers to be sent with the request 
    # + return - FixedAssetV2Entity deleted 
    remote isolated function deleteFixedAssetsV2(string dataAreaId, string fixedAssetNumber, DeleteFixedAssetsV2Headers headers = {}) returns error? {
        string resourcePath = string `/FixedAssetsV2(dataAreaId=${getEncodedUri(dataAreaId)},FixedAssetNumber=${getEncodedUri(fixedAssetNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update FixedAssetV2Entity
    #
    # + dataAreaId - The company data area identifier 
    # + fixedAssetNumber - The fixed asset number key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - FixedAssetV2Entity updated 
    remote isolated function updateFixedAssetsV2(string dataAreaId, string fixedAssetNumber, FixedAssetV2Entity payload, UpdateFixedAssetsV2Headers headers = {}) returns FixedAssetV2Entity|error {
        string resourcePath = string `/FixedAssetsV2(dataAreaId=${getEncodedUri(dataAreaId)},FixedAssetNumber=${getEncodedUri(fixedAssetNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List LeasingGroups
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of LeasingGroup 
    remote isolated function listLeasingGroups(map<string|string[]> headers = {}, *ListLeasingGroupsQueries queries) returns LeasingGroupsCollection|error {
        string resourcePath = string `/LeasingGroups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create LeasingGroup
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - LeasingGroup created 
    remote isolated function createLeasingGroups(LeasingGroup payload, map<string|string[]> headers = {}) returns LeasingGroup|error {
        string resourcePath = string `/LeasingGroups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get LeasingGroup by key
    #
    # + dataAreaId - The company data area identifier 
    # + leaseGroup - The lease group key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - LeasingGroup 
    remote isolated function getLeasingGroups(string dataAreaId, string leaseGroup, map<string|string[]> headers = {}, *GetLeasingGroupsQueries queries) returns LeasingGroup|error {
        string resourcePath = string `/LeasingGroups(dataAreaId=${getEncodedUri(dataAreaId)},LeaseGroup=${getEncodedUri(leaseGroup)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete LeasingGroup
    #
    # + dataAreaId - The company data area identifier 
    # + leaseGroup - The lease group key field 
    # + headers - Headers to be sent with the request 
    # + return - LeasingGroup deleted 
    remote isolated function deleteLeasingGroups(string dataAreaId, string leaseGroup, DeleteLeasingGroupsHeaders headers = {}) returns error? {
        string resourcePath = string `/LeasingGroups(dataAreaId=${getEncodedUri(dataAreaId)},LeaseGroup=${getEncodedUri(leaseGroup)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update LeasingGroup
    #
    # + dataAreaId - The company data area identifier 
    # + leaseGroup - The lease group key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - LeasingGroup updated 
    remote isolated function updateLeasingGroups(string dataAreaId, string leaseGroup, LeasingGroup payload, UpdateLeasingGroupsHeaders headers = {}) returns LeasingGroup|error {
        string resourcePath = string `/LeasingGroups(dataAreaId=${getEncodedUri(dataAreaId)},LeaseGroup=${getEncodedUri(leaseGroup)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List MileageRates
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of MileageRate 
    remote isolated function listMileageRates(map<string|string[]> headers = {}, *ListMileageRatesQueries queries) returns MileageRatesCollection|error {
        string resourcePath = string `/MileageRates`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create MileageRate
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - MileageRate created 
    remote isolated function createMileageRates(MileageRate payload, map<string|string[]> headers = {}) returns MileageRate|error {
        string resourcePath = string `/MileageRates`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get MileageRate by key
    #
    # + dataAreaId - The company data area identifier 
    # + expenseCategory - The expense category key field 
    # + fromDate - The from date key field 
    # + quantity - The quantity key field 
    # + rateType - The rate type key field 
    # + vehicleType - The vehicle type key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - MileageRate 
    remote isolated function getMileageRates(string dataAreaId, string expenseCategory, string fromDate, decimal quantity, string rateType, string vehicleType, map<string|string[]> headers = {}, *GetMileageRatesQueries queries) returns MileageRate|error {
        string resourcePath = string `/MileageRates(dataAreaId=${getEncodedUri(dataAreaId)},ExpenseCategory=${getEncodedUri(expenseCategory)},FromDate=${getEncodedUri(fromDate)},Quantity=${getEncodedUri(quantity)},RateType=${getEncodedUri(rateType)},VehicleType=${getEncodedUri(vehicleType)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete MileageRate
    #
    # + dataAreaId - The company data area identifier 
    # + expenseCategory - The expense category key field 
    # + fromDate - The from date key field 
    # + quantity - The quantity key field 
    # + rateType - The rate type key field 
    # + vehicleType - The vehicle type key field 
    # + headers - Headers to be sent with the request 
    # + return - MileageRate deleted 
    remote isolated function deleteMileageRates(string dataAreaId, string expenseCategory, string fromDate, decimal quantity, string rateType, string vehicleType, DeleteMileageRatesHeaders headers = {}) returns error? {
        string resourcePath = string `/MileageRates(dataAreaId=${getEncodedUri(dataAreaId)},ExpenseCategory=${getEncodedUri(expenseCategory)},FromDate=${getEncodedUri(fromDate)},Quantity=${getEncodedUri(quantity)},RateType=${getEncodedUri(rateType)},VehicleType=${getEncodedUri(vehicleType)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update MileageRate
    #
    # + dataAreaId - The company data area identifier 
    # + expenseCategory - The expense category key field 
    # + fromDate - The from date key field 
    # + quantity - The quantity key field 
    # + rateType - The rate type key field 
    # + vehicleType - The vehicle type key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - MileageRate updated 
    remote isolated function updateMileageRates(string dataAreaId, string expenseCategory, string fromDate, decimal quantity, string rateType, string vehicleType, MileageRate payload, UpdateMileageRatesHeaders headers = {}) returns MileageRate|error {
        string resourcePath = string `/MileageRates(dataAreaId=${getEncodedUri(dataAreaId)},ExpenseCategory=${getEncodedUri(expenseCategory)},FromDate=${getEncodedUri(fromDate)},Quantity=${getEncodedUri(quantity)},RateType=${getEncodedUri(rateType)},VehicleType=${getEncodedUri(vehicleType)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List PSAActuals
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of PSAActual 
    remote isolated function listPSAActuals(map<string|string[]> headers = {}, *ListPSAActualsQueries queries) returns PSAActualsCollection|error {
        string resourcePath = string `/PSAActuals`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create PSAActual
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PSAActual created 
    remote isolated function createPSAActuals(PSAActual payload, map<string|string[]> headers = {}) returns PSAActual|error {
        string resourcePath = string `/PSAActuals`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get PSAActual by key
    #
    # + dataAreaId - The company data area identifier 
    # + transId - The trans id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - PSAActual 
    remote isolated function getPSAActuals(string dataAreaId, string transId, map<string|string[]> headers = {}, *GetPSAActualsQueries queries) returns PSAActual|error {
        string resourcePath = string `/PSAActuals(dataAreaId=${getEncodedUri(dataAreaId)},TransId=${getEncodedUri(transId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete PSAActual
    #
    # + dataAreaId - The company data area identifier 
    # + transId - The trans id key field 
    # + headers - Headers to be sent with the request 
    # + return - PSAActual deleted 
    remote isolated function deletePSAActuals(string dataAreaId, string transId, DeletePSAActualsHeaders headers = {}) returns error? {
        string resourcePath = string `/PSAActuals(dataAreaId=${getEncodedUri(dataAreaId)},TransId=${getEncodedUri(transId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update PSAActual
    #
    # + dataAreaId - The company data area identifier 
    # + transId - The trans id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PSAActual updated 
    remote isolated function updatePSAActuals(string dataAreaId, string transId, PSAActual payload, UpdatePSAActualsHeaders headers = {}) returns PSAActual|error {
        string resourcePath = string `/PSAActuals(dataAreaId=${getEncodedUri(dataAreaId)},TransId=${getEncodedUri(transId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List PSAForecasts
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of PSAForecast 
    remote isolated function listPSAForecasts(map<string|string[]> headers = {}, *ListPSAForecastsQueries queries) returns PSAForecastsCollection|error {
        string resourcePath = string `/PSAForecasts`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create PSAForecast
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PSAForecast created 
    remote isolated function createPSAForecasts(PSAForecast payload, map<string|string[]> headers = {}) returns PSAForecast|error {
        string resourcePath = string `/PSAForecasts`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get PSAForecast by key
    #
    # + dataAreaId - The company data area identifier 
    # + transId - The trans id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - PSAForecast 
    remote isolated function getPSAForecasts(string dataAreaId, string transId, map<string|string[]> headers = {}, *GetPSAForecastsQueries queries) returns PSAForecast|error {
        string resourcePath = string `/PSAForecasts(dataAreaId=${getEncodedUri(dataAreaId)},TransId=${getEncodedUri(transId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete PSAForecast
    #
    # + dataAreaId - The company data area identifier 
    # + transId - The trans id key field 
    # + headers - Headers to be sent with the request 
    # + return - PSAForecast deleted 
    remote isolated function deletePSAForecasts(string dataAreaId, string transId, DeletePSAForecastsHeaders headers = {}) returns error? {
        string resourcePath = string `/PSAForecasts(dataAreaId=${getEncodedUri(dataAreaId)},TransId=${getEncodedUri(transId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update PSAForecast
    #
    # + dataAreaId - The company data area identifier 
    # + transId - The trans id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PSAForecast updated 
    remote isolated function updatePSAForecasts(string dataAreaId, string transId, PSAForecast payload, UpdatePSAForecastsHeaders headers = {}) returns PSAForecast|error {
        string resourcePath = string `/PSAForecasts(dataAreaId=${getEncodedUri(dataAreaId)},TransId=${getEncodedUri(transId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ParentLeases
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ParentLease 
    remote isolated function listParentLeases(map<string|string[]> headers = {}, *ListParentLeasesQueries queries) returns ParentLeasesCollection|error {
        string resourcePath = string `/ParentLeases`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ParentLease
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ParentLease created 
    remote isolated function createParentLeases(ParentLease payload, map<string|string[]> headers = {}) returns ParentLease|error {
        string resourcePath = string `/ParentLeases`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ParentLease by key
    #
    # + dataAreaId - The company data area identifier 
    # + leaseId - The lease id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ParentLease 
    remote isolated function getParentLeases(string dataAreaId, string leaseId, map<string|string[]> headers = {}, *GetParentLeasesQueries queries) returns ParentLease|error {
        string resourcePath = string `/ParentLeases(dataAreaId=${getEncodedUri(dataAreaId)},LeaseId=${getEncodedUri(leaseId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ParentLease
    #
    # + dataAreaId - The company data area identifier 
    # + leaseId - The lease id key field 
    # + headers - Headers to be sent with the request 
    # + return - ParentLease deleted 
    remote isolated function deleteParentLeases(string dataAreaId, string leaseId, DeleteParentLeasesHeaders headers = {}) returns error? {
        string resourcePath = string `/ParentLeases(dataAreaId=${getEncodedUri(dataAreaId)},LeaseId=${getEncodedUri(leaseId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ParentLease
    #
    # + dataAreaId - The company data area identifier 
    # + leaseId - The lease id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ParentLease updated 
    remote isolated function updateParentLeases(string dataAreaId, string leaseId, ParentLease payload, UpdateParentLeasesHeaders headers = {}) returns ParentLease|error {
        string resourcePath = string `/ParentLeases(dataAreaId=${getEncodedUri(dataAreaId)},LeaseId=${getEncodedUri(leaseId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List PerDiems
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of PerDiems 
    remote isolated function listPerDiems(map<string|string[]> headers = {}, *ListPerDiemsQueries queries) returns PerDiemsCollection|error {
        string resourcePath = string `/PerDiems`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create PerDiems
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PerDiems created 
    remote isolated function createPerDiems(PerDiems payload, map<string|string[]> headers = {}) returns PerDiems|error {
        string resourcePath = string `/PerDiems`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get PerDiems by key
    #
    # + dataAreaId - The company data area identifier 
    # + location - The location key field 
    # + stateId - The state id key field 
    # + countryRegionId - The country region id key field 
    # + dateFrom - The date from key field 
    # + dateTo - The date to key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - PerDiems 
    remote isolated function getPerDiems(string dataAreaId, string location, string stateId, string countryRegionId, string dateFrom, string dateTo, map<string|string[]> headers = {}, *GetPerDiemsQueries queries) returns PerDiems|error {
        string resourcePath = string `/PerDiems(dataAreaId=${getEncodedUri(dataAreaId)},Location=${getEncodedUri(location)},StateId=${getEncodedUri(stateId)},CountryRegionId=${getEncodedUri(countryRegionId)},DateFrom=${getEncodedUri(dateFrom)},DateTo=${getEncodedUri(dateTo)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete PerDiems
    #
    # + dataAreaId - The company data area identifier 
    # + location - The location key field 
    # + stateId - The state id key field 
    # + countryRegionId - The country region id key field 
    # + dateFrom - The date from key field 
    # + dateTo - The date to key field 
    # + headers - Headers to be sent with the request 
    # + return - PerDiems deleted 
    remote isolated function deletePerDiems(string dataAreaId, string location, string stateId, string countryRegionId, string dateFrom, string dateTo, DeletePerDiemsHeaders headers = {}) returns error? {
        string resourcePath = string `/PerDiems(dataAreaId=${getEncodedUri(dataAreaId)},Location=${getEncodedUri(location)},StateId=${getEncodedUri(stateId)},CountryRegionId=${getEncodedUri(countryRegionId)},DateFrom=${getEncodedUri(dateFrom)},DateTo=${getEncodedUri(dateTo)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update PerDiems
    #
    # + dataAreaId - The company data area identifier 
    # + location - The location key field 
    # + stateId - The state id key field 
    # + countryRegionId - The country region id key field 
    # + dateFrom - The date from key field 
    # + dateTo - The date to key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PerDiems updated 
    remote isolated function updatePerDiems(string dataAreaId, string location, string stateId, string countryRegionId, string dateFrom, string dateTo, PerDiems payload, UpdatePerDiemsHeaders headers = {}) returns PerDiems|error {
        string resourcePath = string `/PerDiems(dataAreaId=${getEncodedUri(dataAreaId)},Location=${getEncodedUri(location)},StateId=${getEncodedUri(stateId)},CountryRegionId=${getEncodedUri(countryRegionId)},DateFrom=${getEncodedUri(dateFrom)},DateTo=${getEncodedUri(dateTo)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ProjGrants
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ProjGrant 
    remote isolated function listProjGrants(map<string|string[]> headers = {}, *ListProjGrantsQueries queries) returns ProjGrantsCollection|error {
        string resourcePath = string `/ProjGrants`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ProjGrant
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ProjGrant created 
    remote isolated function createProjGrants(ProjGrant payload, map<string|string[]> headers = {}) returns ProjGrant|error {
        string resourcePath = string `/ProjGrants`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ProjGrant by key
    #
    # + dataAreaId - The company data area identifier 
    # + grantId - The grant id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ProjGrant 
    remote isolated function getProjGrants(string dataAreaId, string grantId, map<string|string[]> headers = {}, *GetProjGrantsQueries queries) returns ProjGrant|error {
        string resourcePath = string `/ProjGrants(dataAreaId=${getEncodedUri(dataAreaId)},GrantId=${getEncodedUri(grantId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ProjGrant
    #
    # + dataAreaId - The company data area identifier 
    # + grantId - The grant id key field 
    # + headers - Headers to be sent with the request 
    # + return - ProjGrant deleted 
    remote isolated function deleteProjGrants(string dataAreaId, string grantId, DeleteProjGrantsHeaders headers = {}) returns error? {
        string resourcePath = string `/ProjGrants(dataAreaId=${getEncodedUri(dataAreaId)},GrantId=${getEncodedUri(grantId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ProjGrant
    #
    # + dataAreaId - The company data area identifier 
    # + grantId - The grant id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ProjGrant updated 
    remote isolated function updateProjGrants(string dataAreaId, string grantId, ProjGrant payload, UpdateProjGrantsHeaders headers = {}) returns ProjGrant|error {
        string resourcePath = string `/ProjGrants(dataAreaId=${getEncodedUri(dataAreaId)},GrantId=${getEncodedUri(grantId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ProjGrantsV2
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ProjGrantV2 
    remote isolated function listProjGrantsV2(map<string|string[]> headers = {}, *ListProjGrantsV2Queries queries) returns ProjGrantsV2Collection|error {
        string resourcePath = string `/ProjGrantsV2`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ProjGrantV2
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ProjGrantV2 created 
    remote isolated function createProjGrantsV2(ProjGrantV2 payload, map<string|string[]> headers = {}) returns ProjGrantV2|error {
        string resourcePath = string `/ProjGrantsV2`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ProjGrantV2 by key
    #
    # + dataAreaId - The company data area identifier 
    # + grantId - The grant id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ProjGrantV2 
    remote isolated function getProjGrantsV2(string dataAreaId, string grantId, map<string|string[]> headers = {}, *GetProjGrantsV2Queries queries) returns ProjGrantV2|error {
        string resourcePath = string `/ProjGrantsV2(dataAreaId=${getEncodedUri(dataAreaId)},GrantId=${getEncodedUri(grantId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ProjGrantV2
    #
    # + dataAreaId - The company data area identifier 
    # + grantId - The grant id key field 
    # + headers - Headers to be sent with the request 
    # + return - ProjGrantV2 deleted 
    remote isolated function deleteProjGrantsV2(string dataAreaId, string grantId, DeleteProjGrantsV2Headers headers = {}) returns error? {
        string resourcePath = string `/ProjGrantsV2(dataAreaId=${getEncodedUri(dataAreaId)},GrantId=${getEncodedUri(grantId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ProjGrantV2
    #
    # + dataAreaId - The company data area identifier 
    # + grantId - The grant id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ProjGrantV2 updated 
    remote isolated function updateProjGrantsV2(string dataAreaId, string grantId, ProjGrantV2 payload, UpdateProjGrantsV2Headers headers = {}) returns ProjGrantV2|error {
        string resourcePath = string `/ProjGrantsV2(dataAreaId=${getEncodedUri(dataAreaId)},GrantId=${getEncodedUri(grantId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ProjectGroups
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ProjectGroup 
    remote isolated function listProjectGroups(map<string|string[]> headers = {}, *ListProjectGroupsQueries queries) returns ProjectGroupsCollection|error {
        string resourcePath = string `/ProjectGroups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ProjectGroup
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ProjectGroup created 
    remote isolated function createProjectGroups(ProjectGroup payload, map<string|string[]> headers = {}) returns ProjectGroup|error {
        string resourcePath = string `/ProjectGroups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ProjectGroup by key
    #
    # + dataAreaId - The company data area identifier 
    # + projectGroup - The project group key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ProjectGroup 
    remote isolated function getProjectGroups(string dataAreaId, string projectGroup, map<string|string[]> headers = {}, *GetProjectGroupsQueries queries) returns ProjectGroup|error {
        string resourcePath = string `/ProjectGroups(dataAreaId=${getEncodedUri(dataAreaId)},ProjectGroup=${getEncodedUri(projectGroup)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ProjectGroup
    #
    # + dataAreaId - The company data area identifier 
    # + projectGroup - The project group key field 
    # + headers - Headers to be sent with the request 
    # + return - ProjectGroup deleted 
    remote isolated function deleteProjectGroups(string dataAreaId, string projectGroup, DeleteProjectGroupsHeaders headers = {}) returns error? {
        string resourcePath = string `/ProjectGroups(dataAreaId=${getEncodedUri(dataAreaId)},ProjectGroup=${getEncodedUri(projectGroup)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ProjectGroup
    #
    # + dataAreaId - The company data area identifier 
    # + projectGroup - The project group key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ProjectGroup updated 
    remote isolated function updateProjectGroups(string dataAreaId, string projectGroup, ProjectGroup payload, UpdateProjectGroupsHeaders headers = {}) returns ProjectGroup|error {
        string resourcePath = string `/ProjectGroups(dataAreaId=${getEncodedUri(dataAreaId)},ProjectGroup=${getEncodedUri(projectGroup)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ProjectStages
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ProjectStage 
    remote isolated function listProjectStages(map<string|string[]> headers = {}, *ListProjectStagesQueries queries) returns ProjectStagesCollection|error {
        string resourcePath = string `/ProjectStages`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ProjectStage
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ProjectStage created 
    remote isolated function createProjectStages(ProjectStage payload, map<string|string[]> headers = {}) returns ProjectStage|error {
        string resourcePath = string `/ProjectStages`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ProjectStage by key
    #
    # + dataAreaId - The company data area identifier 
    # + language - The language key field 
    # + status - The status key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ProjectStage 
    remote isolated function getProjectStages(string dataAreaId, string language, string status, map<string|string[]> headers = {}, *GetProjectStagesQueries queries) returns ProjectStage|error {
        string resourcePath = string `/ProjectStages(dataAreaId=${getEncodedUri(dataAreaId)},Language=${getEncodedUri(language)},Status=${getEncodedUri(status)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ProjectStage
    #
    # + dataAreaId - The company data area identifier 
    # + language - The language key field 
    # + status - The status key field 
    # + headers - Headers to be sent with the request 
    # + return - ProjectStage deleted 
    remote isolated function deleteProjectStages(string dataAreaId, string language, string status, DeleteProjectStagesHeaders headers = {}) returns error? {
        string resourcePath = string `/ProjectStages(dataAreaId=${getEncodedUri(dataAreaId)},Language=${getEncodedUri(language)},Status=${getEncodedUri(status)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ProjectStage
    #
    # + dataAreaId - The company data area identifier 
    # + language - The language key field 
    # + status - The status key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ProjectStage updated 
    remote isolated function updateProjectStages(string dataAreaId, string language, string status, ProjectStage payload, UpdateProjectStagesHeaders headers = {}) returns ProjectStage|error {
        string resourcePath = string `/ProjectStages(dataAreaId=${getEncodedUri(dataAreaId)},Language=${getEncodedUri(language)},Status=${getEncodedUri(status)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ProjectTasks
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ProjectTask 
    remote isolated function listProjectTasks(map<string|string[]> headers = {}, *ListProjectTasksQueries queries) returns ProjectTasksCollection|error {
        string resourcePath = string `/ProjectTasks`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ProjectTask
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ProjectTask created 
    remote isolated function createProjectTasks(ProjectTask payload, map<string|string[]> headers = {}) returns ProjectTask|error {
        string resourcePath = string `/ProjectTasks`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ProjectTask by key
    #
    # + dataAreaId - The company data area identifier 
    # + projectId - The project id key field 
    # + taskId - The task id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ProjectTask 
    remote isolated function getProjectTasks(string dataAreaId, string projectId, string taskId, map<string|string[]> headers = {}, *GetProjectTasksQueries queries) returns ProjectTask|error {
        string resourcePath = string `/ProjectTasks(dataAreaId=${getEncodedUri(dataAreaId)},ProjectId=${getEncodedUri(projectId)},TaskId=${getEncodedUri(taskId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ProjectTask
    #
    # + dataAreaId - The company data area identifier 
    # + projectId - The project id key field 
    # + taskId - The task id key field 
    # + headers - Headers to be sent with the request 
    # + return - ProjectTask deleted 
    remote isolated function deleteProjectTasks(string dataAreaId, string projectId, string taskId, DeleteProjectTasksHeaders headers = {}) returns error? {
        string resourcePath = string `/ProjectTasks(dataAreaId=${getEncodedUri(dataAreaId)},ProjectId=${getEncodedUri(projectId)},TaskId=${getEncodedUri(taskId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ProjectTask
    #
    # + dataAreaId - The company data area identifier 
    # + projectId - The project id key field 
    # + taskId - The task id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ProjectTask updated 
    remote isolated function updateProjectTasks(string dataAreaId, string projectId, string taskId, ProjectTask payload, UpdateProjectTasksHeaders headers = {}) returns ProjectTask|error {
        string resourcePath = string `/ProjectTasks(dataAreaId=${getEncodedUri(dataAreaId)},ProjectId=${getEncodedUri(projectId)},TaskId=${getEncodedUri(taskId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List Projects
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of Project 
    remote isolated function listProjects(map<string|string[]> headers = {}, *ListProjectsQueries queries) returns ProjectsCollection|error {
        string resourcePath = string `/Projects`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create Project
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Project created 
    remote isolated function createProjects(Project payload, map<string|string[]> headers = {}) returns Project|error {
        string resourcePath = string `/Projects`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get Project by key
    #
    # + dataAreaId - The company data area identifier 
    # + projectID - The project id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Project 
    remote isolated function getProjects(string dataAreaId, string projectID, map<string|string[]> headers = {}, *GetProjectsQueries queries) returns Project|error {
        string resourcePath = string `/Projects(dataAreaId=${getEncodedUri(dataAreaId)},ProjectID=${getEncodedUri(projectID)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete Project
    #
    # + dataAreaId - The company data area identifier 
    # + projectID - The project id key field 
    # + headers - Headers to be sent with the request 
    # + return - Project deleted 
    remote isolated function deleteProjects(string dataAreaId, string projectID, DeleteProjectsHeaders headers = {}) returns error? {
        string resourcePath = string `/Projects(dataAreaId=${getEncodedUri(dataAreaId)},ProjectID=${getEncodedUri(projectID)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update Project
    #
    # + dataAreaId - The company data area identifier 
    # + projectID - The project id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Project updated 
    remote isolated function updateProjects(string dataAreaId, string projectID, Project payload, UpdateProjectsHeaders headers = {}) returns Project|error {
        string resourcePath = string `/Projects(dataAreaId=${getEncodedUri(dataAreaId)},ProjectID=${getEncodedUri(projectID)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ProjectsV2
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ProjectV2 
    remote isolated function listProjectsV2(map<string|string[]> headers = {}, *ListProjectsV2Queries queries) returns ProjectsV2Collection|error {
        string resourcePath = string `/ProjectsV2`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ProjectV2
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ProjectV2 created 
    remote isolated function createProjectsV2(ProjectV2 payload, map<string|string[]> headers = {}) returns ProjectV2|error {
        string resourcePath = string `/ProjectsV2`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ProjectV2 by key
    #
    # + dataAreaId - The company data area identifier 
    # + projectId - The project id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ProjectV2 
    remote isolated function getProjectsV2(string dataAreaId, string projectId, map<string|string[]> headers = {}, *GetProjectsV2Queries queries) returns ProjectV2|error {
        string resourcePath = string `/ProjectsV2(dataAreaId=${getEncodedUri(dataAreaId)},ProjectId=${getEncodedUri(projectId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ProjectV2
    #
    # + dataAreaId - The company data area identifier 
    # + projectId - The project id key field 
    # + headers - Headers to be sent with the request 
    # + return - ProjectV2 deleted 
    remote isolated function deleteProjectsV2(string dataAreaId, string projectId, DeleteProjectsV2Headers headers = {}) returns error? {
        string resourcePath = string `/ProjectsV2(dataAreaId=${getEncodedUri(dataAreaId)},ProjectId=${getEncodedUri(projectId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ProjectV2
    #
    # + dataAreaId - The company data area identifier 
    # + projectId - The project id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ProjectV2 updated 
    remote isolated function updateProjectsV2(string dataAreaId, string projectId, ProjectV2 payload, UpdateProjectsV2Headers headers = {}) returns ProjectV2|error {
        string resourcePath = string `/ProjectsV2(dataAreaId=${getEncodedUri(dataAreaId)},ProjectId=${getEncodedUri(projectId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List RAssetGroups
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of RAssetGroup 
    remote isolated function listRAssetGroups(map<string|string[]> headers = {}, *ListRAssetGroupsQueries queries) returns RAssetGroupsCollection|error {
        string resourcePath = string `/RAssetGroups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create RAssetGroup
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - RAssetGroup created 
    remote isolated function createRAssetGroups(RAssetGroup payload, map<string|string[]> headers = {}) returns RAssetGroup|error {
        string resourcePath = string `/RAssetGroups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get RAssetGroup by key
    #
    # + dataAreaId - The company data area identifier 
    # + depreciationGroup - The depreciation group key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - RAssetGroup 
    remote isolated function getRAssetGroups(string dataAreaId, string depreciationGroup, map<string|string[]> headers = {}, *GetRAssetGroupsQueries queries) returns RAssetGroup|error {
        string resourcePath = string `/RAssetGroups(dataAreaId=${getEncodedUri(dataAreaId)},DepreciationGroup=${getEncodedUri(depreciationGroup)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete RAssetGroup
    #
    # + dataAreaId - The company data area identifier 
    # + depreciationGroup - The depreciation group key field 
    # + headers - Headers to be sent with the request 
    # + return - RAssetGroup deleted 
    remote isolated function deleteRAssetGroups(string dataAreaId, string depreciationGroup, DeleteRAssetGroupsHeaders headers = {}) returns error? {
        string resourcePath = string `/RAssetGroups(dataAreaId=${getEncodedUri(dataAreaId)},DepreciationGroup=${getEncodedUri(depreciationGroup)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update RAssetGroup
    #
    # + dataAreaId - The company data area identifier 
    # + depreciationGroup - The depreciation group key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - RAssetGroup updated 
    remote isolated function updateRAssetGroups(string dataAreaId, string depreciationGroup, RAssetGroup payload, UpdateRAssetGroupsHeaders headers = {}) returns RAssetGroup|error {
        string resourcePath = string `/RAssetGroups(dataAreaId=${getEncodedUri(dataAreaId)},DepreciationGroup=${getEncodedUri(depreciationGroup)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List RAssetLedgers
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of RAssetLedger 
    remote isolated function listRAssetLedgers(map<string|string[]> headers = {}, *ListRAssetLedgersQueries queries) returns RAssetLedgersCollection|error {
        string resourcePath = string `/RAssetLedgers`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create RAssetLedger
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - RAssetLedger created 
    remote isolated function createRAssetLedgers(RAssetLedger payload, map<string|string[]> headers = {}) returns RAssetLedger|error {
        string resourcePath = string `/RAssetLedgers`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get RAssetLedger by key
    #
    # + dataAreaId - The company data area identifier 
    # + postingProfile - The posting profile key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - RAssetLedger 
    remote isolated function getRAssetLedgers(string dataAreaId, string postingProfile, map<string|string[]> headers = {}, *GetRAssetLedgersQueries queries) returns RAssetLedger|error {
        string resourcePath = string `/RAssetLedgers(dataAreaId=${getEncodedUri(dataAreaId)},PostingProfile=${getEncodedUri(postingProfile)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete RAssetLedger
    #
    # + dataAreaId - The company data area identifier 
    # + postingProfile - The posting profile key field 
    # + headers - Headers to be sent with the request 
    # + return - RAssetLedger deleted 
    remote isolated function deleteRAssetLedgers(string dataAreaId, string postingProfile, DeleteRAssetLedgersHeaders headers = {}) returns error? {
        string resourcePath = string `/RAssetLedgers(dataAreaId=${getEncodedUri(dataAreaId)},PostingProfile=${getEncodedUri(postingProfile)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update RAssetLedger
    #
    # + dataAreaId - The company data area identifier 
    # + postingProfile - The posting profile key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - RAssetLedger updated 
    remote isolated function updateRAssetLedgers(string dataAreaId, string postingProfile, RAssetLedger payload, UpdateRAssetLedgersHeaders headers = {}) returns RAssetLedger|error {
        string resourcePath = string `/RAssetLedgers(dataAreaId=${getEncodedUri(dataAreaId)},PostingProfile=${getEncodedUri(postingProfile)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List RAssetTables
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of RAssetTable 
    remote isolated function listRAssetTables(map<string|string[]> headers = {}, *ListRAssetTablesQueries queries) returns RAssetTablesCollection|error {
        string resourcePath = string `/RAssetTables`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create RAssetTable
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - RAssetTable created 
    remote isolated function createRAssetTables(RAssetTable payload, map<string|string[]> headers = {}) returns RAssetTable|error {
        string resourcePath = string `/RAssetTables`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get RAssetTable by key
    #
    # + dataAreaId - The company data area identifier 
    # + fAInventoryNumber - The f a inventory number key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - RAssetTable 
    remote isolated function getRAssetTables(string dataAreaId, string fAInventoryNumber, map<string|string[]> headers = {}, *GetRAssetTablesQueries queries) returns RAssetTable|error {
        string resourcePath = string `/RAssetTables(dataAreaId=${getEncodedUri(dataAreaId)},FAInventoryNumber=${getEncodedUri(fAInventoryNumber)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete RAssetTable
    #
    # + dataAreaId - The company data area identifier 
    # + fAInventoryNumber - The f a inventory number key field 
    # + headers - Headers to be sent with the request 
    # + return - RAssetTable deleted 
    remote isolated function deleteRAssetTables(string dataAreaId, string fAInventoryNumber, DeleteRAssetTablesHeaders headers = {}) returns error? {
        string resourcePath = string `/RAssetTables(dataAreaId=${getEncodedUri(dataAreaId)},FAInventoryNumber=${getEncodedUri(fAInventoryNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update RAssetTable
    #
    # + dataAreaId - The company data area identifier 
    # + fAInventoryNumber - The f a inventory number key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - RAssetTable updated 
    remote isolated function updateRAssetTables(string dataAreaId, string fAInventoryNumber, RAssetTable payload, UpdateRAssetTablesHeaders headers = {}) returns RAssetTable|error {
        string resourcePath = string `/RAssetTables(dataAreaId=${getEncodedUri(dataAreaId)},FAInventoryNumber=${getEncodedUri(fAInventoryNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List RAssetUses
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of RAssetUse 
    remote isolated function listRAssetUses(map<string|string[]> headers = {}, *ListRAssetUsesQueries queries) returns RAssetUsesCollection|error {
        string resourcePath = string `/RAssetUses`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create RAssetUse
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - RAssetUse created 
    remote isolated function createRAssetUses(RAssetUse payload, map<string|string[]> headers = {}) returns RAssetUse|error {
        string resourcePath = string `/RAssetUses`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get RAssetUse by key
    #
    # + dataAreaId - The company data area identifier 
    # + fAInventoryNumber - The f a inventory number key field 
    # + period - The period key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - RAssetUse 
    remote isolated function getRAssetUses(string dataAreaId, string fAInventoryNumber, string period, map<string|string[]> headers = {}, *GetRAssetUsesQueries queries) returns RAssetUse|error {
        string resourcePath = string `/RAssetUses(dataAreaId=${getEncodedUri(dataAreaId)},FAInventoryNumber=${getEncodedUri(fAInventoryNumber)},Period=${getEncodedUri(period)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete RAssetUse
    #
    # + dataAreaId - The company data area identifier 
    # + fAInventoryNumber - The f a inventory number key field 
    # + period - The period key field 
    # + headers - Headers to be sent with the request 
    # + return - RAssetUse deleted 
    remote isolated function deleteRAssetUses(string dataAreaId, string fAInventoryNumber, string period, DeleteRAssetUsesHeaders headers = {}) returns error? {
        string resourcePath = string `/RAssetUses(dataAreaId=${getEncodedUri(dataAreaId)},FAInventoryNumber=${getEncodedUri(fAInventoryNumber)},Period=${getEncodedUri(period)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update RAssetUse
    #
    # + dataAreaId - The company data area identifier 
    # + fAInventoryNumber - The f a inventory number key field 
    # + period - The period key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - RAssetUse updated 
    remote isolated function updateRAssetUses(string dataAreaId, string fAInventoryNumber, string period, RAssetUse payload, UpdateRAssetUsesHeaders headers = {}) returns RAssetUse|error {
        string resourcePath = string `/RAssetUses(dataAreaId=${getEncodedUri(dataAreaId)},FAInventoryNumber=${getEncodedUri(fAInventoryNumber)},Period=${getEncodedUri(period)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List TrvReceipts
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of TrvReceiptEntity 
    remote isolated function listTrvReceipts(map<string|string[]> headers = {}, *ListTrvReceiptsQueries queries) returns TrvReceiptsCollection|error {
        string resourcePath = string `/TrvReceipts`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create TrvReceiptEntity
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - TrvReceiptEntity created 
    remote isolated function createTrvReceipts(TrvReceiptEntity payload, map<string|string[]> headers = {}) returns TrvReceiptEntity|error {
        string resourcePath = string `/TrvReceipts`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get TrvReceiptEntity by key
    #
    # + dataAreaId - The company data area identifier 
    # + documentId - The document id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - TrvReceiptEntity 
    remote isolated function getTrvReceipts(string dataAreaId, string documentId, map<string|string[]> headers = {}, *GetTrvReceiptsQueries queries) returns TrvReceiptEntity|error {
        string resourcePath = string `/TrvReceipts(dataAreaId=${getEncodedUri(dataAreaId)},DocumentId=${getEncodedUri(documentId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete TrvReceiptEntity
    #
    # + dataAreaId - The company data area identifier 
    # + documentId - The document id key field 
    # + headers - Headers to be sent with the request 
    # + return - TrvReceiptEntity deleted 
    remote isolated function deleteTrvReceipts(string dataAreaId, string documentId, DeleteTrvReceiptsHeaders headers = {}) returns error? {
        string resourcePath = string `/TrvReceipts(dataAreaId=${getEncodedUri(dataAreaId)},DocumentId=${getEncodedUri(documentId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update TrvReceiptEntity
    #
    # + dataAreaId - The company data area identifier 
    # + documentId - The document id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - TrvReceiptEntity updated 
    remote isolated function updateTrvReceipts(string dataAreaId, string documentId, TrvReceiptEntity payload, UpdateTrvReceiptsHeaders headers = {}) returns TrvReceiptEntity|error {
        string resourcePath = string `/TrvReceipts(dataAreaId=${getEncodedUri(dataAreaId)},DocumentId=${getEncodedUri(documentId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }
}
