// AUTO-GENERATED FILE. DO NOT MODIFY.
// This file is auto-generated by the Ballerina OpenAPI tool.

import ballerina/data.jsondata;
import ballerina/http;

public isolated client class Client {
    final http:Client clientEp;
    # Gets invoked to initialize the `connector`.
    #
    # + config - The configurations to be used when initializing the `connector` 
    # + serviceUrl - URL of the target service 
    # + return - An error if connector initialization failed 
    public isolated function init(ConnectionConfig config, string serviceUrl = "https://your-org.operations.dynamics.com/data") returns error? {
        http:ClientConfiguration httpClientConfig = {auth: config.auth, httpVersion: config.httpVersion, http1Settings: config.http1Settings, http2Settings: config.http2Settings, timeout: config.timeout, forwarded: config.forwarded, followRedirects: config.followRedirects, poolConfig: config.poolConfig, cache: config.cache, compression: config.compression, circuitBreaker: config.circuitBreaker, retryConfig: config.retryConfig, cookieConfig: config.cookieConfig, responseLimits: config.responseLimits, secureSocket: config.secureSocket, proxy: config.proxy, socketConfig: config.socketConfig, validation: config.validation, laxDataBinding: config.laxDataBinding};
        self.clientEp = check new (serviceUrl, httpClientConfig);
    }

    # List AddressBooks
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of AddressBooks 
    remote isolated function listAddressBooks(map<string|string[]> headers = {}, *ListAddressBooksQueries queries) returns AddressBooksCollection|error {
        string resourcePath = string `/AddressBooks`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create AddressBooks
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AddressBooks created 
    remote isolated function createAddressBooks(AddressBooks payload, map<string|string[]> headers = {}) returns AddressBooks|error {
        string resourcePath = string `/AddressBooks`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get AddressBooks by key
    #
    # + name - The name key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - AddressBooks 
    remote isolated function getAddressBooks(string name, map<string|string[]> headers = {}, *GetAddressBooksQueries queries) returns AddressBooks|error {
        string resourcePath = string `/AddressBooks(${getEncodedUri(name)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete AddressBooks
    #
    # + name - The name key field 
    # + headers - Headers to be sent with the request 
    # + return - AddressBooks deleted 
    remote isolated function deleteAddressBooks(string name, DeleteAddressBooksHeaders headers = {}) returns error? {
        string resourcePath = string `/AddressBooks(${getEncodedUri(name)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update AddressBooks
    #
    # + name - The name key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AddressBooks updated 
    remote isolated function updateAddressBooks(string name, AddressBooks payload, UpdateAddressBooksHeaders headers = {}) returns AddressBooks|error {
        string resourcePath = string `/AddressBooks(${getEncodedUri(name)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List AddressCities
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of AddressCity 
    remote isolated function listAddressCities(map<string|string[]> headers = {}, *ListAddressCitiesQueries queries) returns AddressCitiesCollection|error {
        string resourcePath = string `/AddressCities`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create AddressCity
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AddressCity created 
    remote isolated function createAddressCities(AddressCity payload, map<string|string[]> headers = {}) returns AddressCity|error {
        string resourcePath = string `/AddressCities`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get AddressCity by key
    #
    # + cityKey - The city key key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - AddressCity 
    remote isolated function getAddressCities(string cityKey, map<string|string[]> headers = {}, *GetAddressCitiesQueries queries) returns AddressCity|error {
        string resourcePath = string `/AddressCities(${getEncodedUri(cityKey)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete AddressCity
    #
    # + cityKey - The city key key field 
    # + headers - Headers to be sent with the request 
    # + return - AddressCity deleted 
    remote isolated function deleteAddressCities(string cityKey, DeleteAddressCitiesHeaders headers = {}) returns error? {
        string resourcePath = string `/AddressCities(${getEncodedUri(cityKey)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update AddressCity
    #
    # + cityKey - The city key key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AddressCity updated 
    remote isolated function updateAddressCities(string cityKey, AddressCity payload, UpdateAddressCitiesHeaders headers = {}) returns AddressCity|error {
        string resourcePath = string `/AddressCities(${getEncodedUri(cityKey)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List AddressFormats
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of AddressFormat 
    remote isolated function listAddressFormats(map<string|string[]> headers = {}, *ListAddressFormatsQueries queries) returns AddressFormatsCollection|error {
        string resourcePath = string `/AddressFormats`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create AddressFormat
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AddressFormat created 
    remote isolated function createAddressFormats(AddressFormat payload, map<string|string[]> headers = {}) returns AddressFormat|error {
        string resourcePath = string `/AddressFormats`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get AddressFormat by key
    #
    # + addressFormat - The address format key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - AddressFormat 
    remote isolated function getAddressFormats(string addressFormat, map<string|string[]> headers = {}, *GetAddressFormatsQueries queries) returns AddressFormat|error {
        string resourcePath = string `/AddressFormats(${getEncodedUri(addressFormat)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete AddressFormat
    #
    # + addressFormat - The address format key field 
    # + headers - Headers to be sent with the request 
    # + return - AddressFormat deleted 
    remote isolated function deleteAddressFormats(string addressFormat, DeleteAddressFormatsHeaders headers = {}) returns error? {
        string resourcePath = string `/AddressFormats(${getEncodedUri(addressFormat)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update AddressFormat
    #
    # + addressFormat - The address format key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AddressFormat updated 
    remote isolated function updateAddressFormats(string addressFormat, AddressFormat payload, UpdateAddressFormatsHeaders headers = {}) returns AddressFormat|error {
        string resourcePath = string `/AddressFormats(${getEncodedUri(addressFormat)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List AddressObjects
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of AddressObject 
    remote isolated function listAddressObjects(map<string|string[]> headers = {}, *ListAddressObjectsQueries queries) returns AddressObjectsCollection|error {
        string resourcePath = string `/AddressObjects`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create AddressObject
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AddressObject created 
    remote isolated function createAddressObjects(AddressObject payload, map<string|string[]> headers = {}) returns AddressObject|error {
        string resourcePath = string `/AddressObjects`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get AddressObject by key
    #
    # + dataAreaId - The company data area identifier 
    # + aOId - The a o id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - AddressObject 
    remote isolated function getAddressObjects(string dataAreaId, string aOId, map<string|string[]> headers = {}, *GetAddressObjectsQueries queries) returns AddressObject|error {
        string resourcePath = string `/AddressObjects(dataAreaId=${getEncodedUri(dataAreaId)},AOId=${getEncodedUri(aOId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete AddressObject
    #
    # + dataAreaId - The company data area identifier 
    # + aOId - The a o id key field 
    # + headers - Headers to be sent with the request 
    # + return - AddressObject deleted 
    remote isolated function deleteAddressObjects(string dataAreaId, string aOId, DeleteAddressObjectsHeaders headers = {}) returns error? {
        string resourcePath = string `/AddressObjects(dataAreaId=${getEncodedUri(dataAreaId)},AOId=${getEncodedUri(aOId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update AddressObject
    #
    # + dataAreaId - The company data area identifier 
    # + aOId - The a o id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AddressObject updated 
    remote isolated function updateAddressObjects(string dataAreaId, string aOId, AddressObject payload, UpdateAddressObjectsHeaders headers = {}) returns AddressObject|error {
        string resourcePath = string `/AddressObjects(dataAreaId=${getEncodedUri(dataAreaId)},AOId=${getEncodedUri(aOId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List AddressStates
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of AddressState 
    remote isolated function listAddressStates(map<string|string[]> headers = {}, *ListAddressStatesQueries queries) returns AddressStatesCollection|error {
        string resourcePath = string `/AddressStates`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create AddressState
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AddressState created 
    remote isolated function createAddressStates(AddressState payload, map<string|string[]> headers = {}) returns AddressState|error {
        string resourcePath = string `/AddressStates`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get AddressState by key
    #
    # + countryRegionId - The country region id key field 
    # + state - The state key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - AddressState 
    remote isolated function getAddressStates(string countryRegionId, string state, map<string|string[]> headers = {}, *GetAddressStatesQueries queries) returns AddressState|error {
        string resourcePath = string `/AddressStates(CountryRegionId=${getEncodedUri(countryRegionId)},State=${getEncodedUri(state)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete AddressState
    #
    # + countryRegionId - The country region id key field 
    # + state - The state key field 
    # + headers - Headers to be sent with the request 
    # + return - AddressState deleted 
    remote isolated function deleteAddressStates(string countryRegionId, string state, DeleteAddressStatesHeaders headers = {}) returns error? {
        string resourcePath = string `/AddressStates(CountryRegionId=${getEncodedUri(countryRegionId)},State=${getEncodedUri(state)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update AddressState
    #
    # + countryRegionId - The country region id key field 
    # + state - The state key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - AddressState updated 
    remote isolated function updateAddressStates(string countryRegionId, string state, AddressState payload, UpdateAddressStatesHeaders headers = {}) returns AddressState|error {
        string resourcePath = string `/AddressStates(CountryRegionId=${getEncodedUri(countryRegionId)},State=${getEncodedUri(state)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List Branches
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of Branch 
    remote isolated function listBranches(map<string|string[]> headers = {}, *ListBranchesQueries queries) returns BranchesCollection|error {
        string resourcePath = string `/Branches`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create Branch
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Branch created 
    remote isolated function createBranches(Branch payload, map<string|string[]> headers = {}) returns Branch|error {
        string resourcePath = string `/Branches`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get Branch by key
    #
    # + dataAreaId - The company data area identifier 
    # + code - The code key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Branch 
    remote isolated function getBranches(string dataAreaId, string code, map<string|string[]> headers = {}, *GetBranchesQueries queries) returns Branch|error {
        string resourcePath = string `/Branches(dataAreaId=${getEncodedUri(dataAreaId)},Code=${getEncodedUri(code)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete Branch
    #
    # + dataAreaId - The company data area identifier 
    # + code - The code key field 
    # + headers - Headers to be sent with the request 
    # + return - Branch deleted 
    remote isolated function deleteBranches(string dataAreaId, string code, DeleteBranchesHeaders headers = {}) returns error? {
        string resourcePath = string `/Branches(dataAreaId=${getEncodedUri(dataAreaId)},Code=${getEncodedUri(code)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update Branch
    #
    # + dataAreaId - The company data area identifier 
    # + code - The code key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Branch updated 
    remote isolated function updateBranches(string dataAreaId, string code, Branch payload, UpdateBranchesHeaders headers = {}) returns Branch|error {
        string resourcePath = string `/Branches(dataAreaId=${getEncodedUri(dataAreaId)},Code=${getEncodedUri(code)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List BusinessUnits
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of BusinessUnit 
    remote isolated function listBusinessUnits(map<string|string[]> headers = {}, *ListBusinessUnitsQueries queries) returns BusinessUnitsCollection|error {
        string resourcePath = string `/BusinessUnits`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create BusinessUnit
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - BusinessUnit created 
    remote isolated function createBusinessUnits(BusinessUnit payload, map<string|string[]> headers = {}) returns BusinessUnit|error {
        string resourcePath = string `/BusinessUnits`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get BusinessUnit by key
    #
    # + operatingUnitNumber - The operating unit number key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - BusinessUnit 
    remote isolated function getBusinessUnits(string operatingUnitNumber, map<string|string[]> headers = {}, *GetBusinessUnitsQueries queries) returns BusinessUnit|error {
        string resourcePath = string `/BusinessUnits(${getEncodedUri(operatingUnitNumber)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete BusinessUnit
    #
    # + operatingUnitNumber - The operating unit number key field 
    # + headers - Headers to be sent with the request 
    # + return - BusinessUnit deleted 
    remote isolated function deleteBusinessUnits(string operatingUnitNumber, DeleteBusinessUnitsHeaders headers = {}) returns error? {
        string resourcePath = string `/BusinessUnits(${getEncodedUri(operatingUnitNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update BusinessUnit
    #
    # + operatingUnitNumber - The operating unit number key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - BusinessUnit updated 
    remote isolated function updateBusinessUnits(string operatingUnitNumber, BusinessUnit payload, UpdateBusinessUnitsHeaders headers = {}) returns BusinessUnit|error {
        string resourcePath = string `/BusinessUnits(${getEncodedUri(operatingUnitNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List CDSParties
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of CDSParty 
    remote isolated function listCDSParties(map<string|string[]> headers = {}, *ListCDSPartiesQueries queries) returns CDSPartiesCollection|error {
        string resourcePath = string `/CDSParties`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create CDSParty
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CDSParty created 
    remote isolated function createCDSParties(CDSParty payload, map<string|string[]> headers = {}) returns CDSParty|error {
        string resourcePath = string `/CDSParties`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get CDSParty by key
    #
    # + partyNumber - The party number key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CDSParty 
    remote isolated function getCDSParties(string partyNumber, map<string|string[]> headers = {}, *GetCDSPartiesQueries queries) returns CDSParty|error {
        string resourcePath = string `/CDSParties(${getEncodedUri(partyNumber)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete CDSParty
    #
    # + partyNumber - The party number key field 
    # + headers - Headers to be sent with the request 
    # + return - CDSParty deleted 
    remote isolated function deleteCDSParties(string partyNumber, DeleteCDSPartiesHeaders headers = {}) returns error? {
        string resourcePath = string `/CDSParties(${getEncodedUri(partyNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update CDSParty
    #
    # + partyNumber - The party number key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CDSParty updated 
    remote isolated function updateCDSParties(string partyNumber, CDSParty payload, UpdateCDSPartiesHeaders headers = {}) returns CDSParty|error {
        string resourcePath = string `/CDSParties(${getEncodedUri(partyNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List CardTypes
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of CardTypes 
    remote isolated function listCardTypes(map<string|string[]> headers = {}, *ListCardTypesQueries queries) returns CardTypesCollection|error {
        string resourcePath = string `/CardTypes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create CardTypes
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CardTypes created 
    remote isolated function createCardTypes(CardTypes payload, map<string|string[]> headers = {}) returns CardTypes|error {
        string resourcePath = string `/CardTypes`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get CardTypes by key
    #
    # + dataAreaId - The company data area identifier 
    # + trvCreditCardType - The trv credit card type key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CardTypes 
    remote isolated function getCardTypes(string dataAreaId, string trvCreditCardType, map<string|string[]> headers = {}, *GetCardTypesQueries queries) returns CardTypes|error {
        string resourcePath = string `/CardTypes(dataAreaId=${getEncodedUri(dataAreaId)},TrvCreditCardType=${getEncodedUri(trvCreditCardType)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete CardTypes
    #
    # + dataAreaId - The company data area identifier 
    # + trvCreditCardType - The trv credit card type key field 
    # + headers - Headers to be sent with the request 
    # + return - CardTypes deleted 
    remote isolated function deleteCardTypes(string dataAreaId, string trvCreditCardType, DeleteCardTypesHeaders headers = {}) returns error? {
        string resourcePath = string `/CardTypes(dataAreaId=${getEncodedUri(dataAreaId)},TrvCreditCardType=${getEncodedUri(trvCreditCardType)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update CardTypes
    #
    # + dataAreaId - The company data area identifier 
    # + trvCreditCardType - The trv credit card type key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CardTypes updated 
    remote isolated function updateCardTypes(string dataAreaId, string trvCreditCardType, CardTypes payload, UpdateCardTypesHeaders headers = {}) returns CardTypes|error {
        string resourcePath = string `/CardTypes(dataAreaId=${getEncodedUri(dataAreaId)},TrvCreditCardType=${getEncodedUri(trvCreditCardType)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List Categories
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of Category 
    remote isolated function listCategories(map<string|string[]> headers = {}, *ListCategoriesQueries queries) returns CategoriesCollection|error {
        string resourcePath = string `/Categories`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create Category
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Category created 
    remote isolated function createCategories(Category payload, map<string|string[]> headers = {}) returns Category|error {
        string resourcePath = string `/Categories`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get Category by key
    #
    # + dataAreaId - The company data area identifier 
    # + categoryId - The category id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Category 
    remote isolated function getCategories(string dataAreaId, string categoryId, map<string|string[]> headers = {}, *GetCategoriesQueries queries) returns Category|error {
        string resourcePath = string `/Categories(dataAreaId=${getEncodedUri(dataAreaId)},CategoryId=${getEncodedUri(categoryId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete Category
    #
    # + dataAreaId - The company data area identifier 
    # + categoryId - The category id key field 
    # + headers - Headers to be sent with the request 
    # + return - Category deleted 
    remote isolated function deleteCategories(string dataAreaId, string categoryId, DeleteCategoriesHeaders headers = {}) returns error? {
        string resourcePath = string `/Categories(dataAreaId=${getEncodedUri(dataAreaId)},CategoryId=${getEncodedUri(categoryId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update Category
    #
    # + dataAreaId - The company data area identifier 
    # + categoryId - The category id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Category updated 
    remote isolated function updateCategories(string dataAreaId, string categoryId, Category payload, UpdateCategoriesHeaders headers = {}) returns Category|error {
        string resourcePath = string `/Categories(dataAreaId=${getEncodedUri(dataAreaId)},CategoryId=${getEncodedUri(categoryId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List Companies
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of Company 
    remote isolated function listCompanies(map<string|string[]> headers = {}, *ListCompaniesQueries queries) returns CompaniesCollection|error {
        string resourcePath = string `/Companies`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create Company
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Company created 
    remote isolated function createCompanies(Company payload, map<string|string[]> headers = {}) returns Company|error {
        string resourcePath = string `/Companies`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get Company by key
    #
    # + dataArea - The data area key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Company 
    remote isolated function getCompanies(string dataArea, map<string|string[]> headers = {}, *GetCompaniesQueries queries) returns Company|error {
        string resourcePath = string `/Companies(${getEncodedUri(dataArea)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete Company
    #
    # + dataArea - The data area key field 
    # + headers - Headers to be sent with the request 
    # + return - Company deleted 
    remote isolated function deleteCompanies(string dataArea, DeleteCompaniesHeaders headers = {}) returns error? {
        string resourcePath = string `/Companies(${getEncodedUri(dataArea)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update Company
    #
    # + dataArea - The data area key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Company updated 
    remote isolated function updateCompanies(string dataArea, Company payload, UpdateCompaniesHeaders headers = {}) returns Company|error {
        string resourcePath = string `/Companies(${getEncodedUri(dataArea)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List Currencies
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of Currency 
    remote isolated function listCurrencies(map<string|string[]> headers = {}, *ListCurrenciesQueries queries) returns CurrenciesCollection|error {
        string resourcePath = string `/Currencies`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create Currency
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Currency created 
    remote isolated function createCurrencies(Currency payload, map<string|string[]> headers = {}) returns Currency|error {
        string resourcePath = string `/Currencies`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get Currency by key
    #
    # + currencyCode - The currency code key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Currency 
    remote isolated function getCurrencies(string currencyCode, map<string|string[]> headers = {}, *GetCurrenciesQueries queries) returns Currency|error {
        string resourcePath = string `/Currencies(${getEncodedUri(currencyCode)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete Currency
    #
    # + currencyCode - The currency code key field 
    # + headers - Headers to be sent with the request 
    # + return - Currency deleted 
    remote isolated function deleteCurrencies(string currencyCode, DeleteCurrenciesHeaders headers = {}) returns error? {
        string resourcePath = string `/Currencies(${getEncodedUri(currencyCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update Currency
    #
    # + currencyCode - The currency code key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Currency updated 
    remote isolated function updateCurrencies(string currencyCode, Currency payload, UpdateCurrenciesHeaders headers = {}) returns Currency|error {
        string resourcePath = string `/Currencies(${getEncodedUri(currencyCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List CurrencyRules
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of CurrencyRule 
    remote isolated function listCurrencyRules(map<string|string[]> headers = {}, *ListCurrencyRulesQueries queries) returns CurrencyRulesCollection|error {
        string resourcePath = string `/CurrencyRules`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create CurrencyRule
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CurrencyRule created 
    remote isolated function createCurrencyRules(CurrencyRule payload, map<string|string[]> headers = {}) returns CurrencyRule|error {
        string resourcePath = string `/CurrencyRules`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get CurrencyRule by key
    #
    # + dataAreaId - The company data area identifier 
    # + ruleGroup - The rule group key field 
    # + currencyRuleGroup - The currency rule group key field 
    # + currency - The currency key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - CurrencyRule 
    remote isolated function getCurrencyRules(string dataAreaId, string ruleGroup, string currencyRuleGroup, string currency, map<string|string[]> headers = {}, *GetCurrencyRulesQueries queries) returns CurrencyRule|error {
        string resourcePath = string `/CurrencyRules(dataAreaId=${getEncodedUri(dataAreaId)},RuleGroup=${getEncodedUri(ruleGroup)},CurrencyRuleGroup=${getEncodedUri(currencyRuleGroup)},Currency=${getEncodedUri(currency)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete CurrencyRule
    #
    # + dataAreaId - The company data area identifier 
    # + ruleGroup - The rule group key field 
    # + currencyRuleGroup - The currency rule group key field 
    # + currency - The currency key field 
    # + headers - Headers to be sent with the request 
    # + return - CurrencyRule deleted 
    remote isolated function deleteCurrencyRules(string dataAreaId, string ruleGroup, string currencyRuleGroup, string currency, DeleteCurrencyRulesHeaders headers = {}) returns error? {
        string resourcePath = string `/CurrencyRules(dataAreaId=${getEncodedUri(dataAreaId)},RuleGroup=${getEncodedUri(ruleGroup)},CurrencyRuleGroup=${getEncodedUri(currencyRuleGroup)},Currency=${getEncodedUri(currency)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update CurrencyRule
    #
    # + dataAreaId - The company data area identifier 
    # + ruleGroup - The rule group key field 
    # + currencyRuleGroup - The currency rule group key field 
    # + currency - The currency key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - CurrencyRule updated 
    remote isolated function updateCurrencyRules(string dataAreaId, string ruleGroup, string currencyRuleGroup, string currency, CurrencyRule payload, UpdateCurrencyRulesHeaders headers = {}) returns CurrencyRule|error {
        string resourcePath = string `/CurrencyRules(dataAreaId=${getEncodedUri(dataAreaId)},RuleGroup=${getEncodedUri(ruleGroup)},CurrencyRuleGroup=${getEncodedUri(currencyRuleGroup)},Currency=${getEncodedUri(currency)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List Denominations
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of Denomination 
    remote isolated function listDenominations(map<string|string[]> headers = {}, *ListDenominationsQueries queries) returns DenominationsCollection|error {
        string resourcePath = string `/Denominations`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create Denomination
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Denomination created 
    remote isolated function createDenominations(Denomination payload, map<string|string[]> headers = {}) returns Denomination|error {
        string resourcePath = string `/Denominations`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get Denomination by key
    #
    # + dataAreaId - The company data area identifier 
    # + name - The name key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Denomination 
    remote isolated function getDenominations(string dataAreaId, string name, map<string|string[]> headers = {}, *GetDenominationsQueries queries) returns Denomination|error {
        string resourcePath = string `/Denominations(dataAreaId=${getEncodedUri(dataAreaId)},Name=${getEncodedUri(name)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete Denomination
    #
    # + dataAreaId - The company data area identifier 
    # + name - The name key field 
    # + headers - Headers to be sent with the request 
    # + return - Denomination deleted 
    remote isolated function deleteDenominations(string dataAreaId, string name, DeleteDenominationsHeaders headers = {}) returns error? {
        string resourcePath = string `/Denominations(dataAreaId=${getEncodedUri(dataAreaId)},Name=${getEncodedUri(name)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update Denomination
    #
    # + dataAreaId - The company data area identifier 
    # + name - The name key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Denomination updated 
    remote isolated function updateDenominations(string dataAreaId, string name, Denomination payload, UpdateDenominationsHeaders headers = {}) returns Denomination|error {
        string resourcePath = string `/Denominations(dataAreaId=${getEncodedUri(dataAreaId)},Name=${getEncodedUri(name)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List Departments
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of Department 
    remote isolated function listDepartments(map<string|string[]> headers = {}, *ListDepartmentsQueries queries) returns DepartmentsCollection|error {
        string resourcePath = string `/Departments`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create Department
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Department created 
    remote isolated function createDepartments(Department payload, map<string|string[]> headers = {}) returns Department|error {
        string resourcePath = string `/Departments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get Department by key
    #
    # + partyNumber - The party number key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Department 
    remote isolated function getDepartments(string partyNumber, map<string|string[]> headers = {}, *GetDepartmentsQueries queries) returns Department|error {
        string resourcePath = string `/Departments(${getEncodedUri(partyNumber)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete Department
    #
    # + partyNumber - The party number key field 
    # + headers - Headers to be sent with the request 
    # + return - Department deleted 
    remote isolated function deleteDepartments(string partyNumber, DeleteDepartmentsHeaders headers = {}) returns error? {
        string resourcePath = string `/Departments(${getEncodedUri(partyNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update Department
    #
    # + partyNumber - The party number key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Department updated 
    remote isolated function updateDepartments(string partyNumber, Department payload, UpdateDepartmentsHeaders headers = {}) returns Department|error {
        string resourcePath = string `/Departments(${getEncodedUri(partyNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List DepartmentsV2
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of DepartmentV2 
    remote isolated function listDepartmentsV2(map<string|string[]> headers = {}, *ListDepartmentsV2Queries queries) returns DepartmentsV2Collection|error {
        string resourcePath = string `/DepartmentsV2`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create DepartmentV2
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - DepartmentV2 created 
    remote isolated function createDepartmentsV2(DepartmentV2 payload, map<string|string[]> headers = {}) returns DepartmentV2|error {
        string resourcePath = string `/DepartmentsV2`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get DepartmentV2 by key
    #
    # + operatingUnitNumber - The operating unit number key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - DepartmentV2 
    remote isolated function getDepartmentsV2(string operatingUnitNumber, map<string|string[]> headers = {}, *GetDepartmentsV2Queries queries) returns DepartmentV2|error {
        string resourcePath = string `/DepartmentsV2(${getEncodedUri(operatingUnitNumber)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete DepartmentV2
    #
    # + operatingUnitNumber - The operating unit number key field 
    # + headers - Headers to be sent with the request 
    # + return - DepartmentV2 deleted 
    remote isolated function deleteDepartmentsV2(string operatingUnitNumber, DeleteDepartmentsV2Headers headers = {}) returns error? {
        string resourcePath = string `/DepartmentsV2(${getEncodedUri(operatingUnitNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update DepartmentV2
    #
    # + operatingUnitNumber - The operating unit number key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - DepartmentV2 updated 
    remote isolated function updateDepartmentsV2(string operatingUnitNumber, DepartmentV2 payload, UpdateDepartmentsV2Headers headers = {}) returns DepartmentV2|error {
        string resourcePath = string `/DepartmentsV2(${getEncodedUri(operatingUnitNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List DirParameters
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of DirParameters 
    remote isolated function listDirParameters(map<string|string[]> headers = {}, *ListDirParametersQueries queries) returns DirParametersCollection|error {
        string resourcePath = string `/DirParameters`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create DirParameters
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - DirParameters created 
    remote isolated function createDirParameters(DirParameters payload, map<string|string[]> headers = {}) returns DirParameters|error {
        string resourcePath = string `/DirParameters`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get DirParameters by key
    #
    # + 'key - The entity key value 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - DirParameters 
    remote isolated function getDirParameters(int 'key, map<string|string[]> headers = {}, *GetDirParametersQueries queries) returns DirParameters|error {
        string resourcePath = string `/DirParameters(${getEncodedUri('key)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete DirParameters
    #
    # + 'key - The entity key value 
    # + headers - Headers to be sent with the request 
    # + return - DirParameters deleted 
    remote isolated function deleteDirParameters(int 'key, DeleteDirParametersHeaders headers = {}) returns error? {
        string resourcePath = string `/DirParameters(${getEncodedUri('key)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update DirParameters
    #
    # + payload - The request body 
    # + 'key - The entity key value 
    # + headers - Headers to be sent with the request 
    # + return - DirParameters updated 
    remote isolated function updateDirParameters(int 'key, DirParameters payload, UpdateDirParametersHeaders headers = {}) returns DirParameters|error {
        string resourcePath = string `/DirParameters(${getEncodedUri('key)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ELCOAs
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ELCOA 
    remote isolated function listELCOAs(map<string|string[]> headers = {}, *ListELCOAsQueries queries) returns ELCOAsCollection|error {
        string resourcePath = string `/ELCOAs`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ELCOA
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ELCOA created 
    remote isolated function createELCOAs(ELCOA payload, map<string|string[]> headers = {}) returns ELCOA|error {
        string resourcePath = string `/ELCOAs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ELCOA by key
    #
    # + dataAreaId - The company data area identifier 
    # + dimensionAttributeValueDisplayValue - The dimension attribute value display value key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ELCOA 
    remote isolated function getELCOAs(string dataAreaId, string dimensionAttributeValueDisplayValue, map<string|string[]> headers = {}, *GetELCOAsQueries queries) returns ELCOA|error {
        string resourcePath = string `/ELCOAs(dataAreaId=${getEncodedUri(dataAreaId)},DimensionAttributeValueDisplayValue=${getEncodedUri(dimensionAttributeValueDisplayValue)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ELCOA
    #
    # + dataAreaId - The company data area identifier 
    # + dimensionAttributeValueDisplayValue - The dimension attribute value display value key field 
    # + headers - Headers to be sent with the request 
    # + return - ELCOA deleted 
    remote isolated function deleteELCOAs(string dataAreaId, string dimensionAttributeValueDisplayValue, DeleteELCOAsHeaders headers = {}) returns error? {
        string resourcePath = string `/ELCOAs(dataAreaId=${getEncodedUri(dataAreaId)},DimensionAttributeValueDisplayValue=${getEncodedUri(dimensionAttributeValueDisplayValue)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ELCOA
    #
    # + dataAreaId - The company data area identifier 
    # + dimensionAttributeValueDisplayValue - The dimension attribute value display value key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ELCOA updated 
    remote isolated function updateELCOAs(string dataAreaId, string dimensionAttributeValueDisplayValue, ELCOA payload, UpdateELCOAsHeaders headers = {}) returns ELCOA|error {
        string resourcePath = string `/ELCOAs(dataAreaId=${getEncodedUri(dataAreaId)},DimensionAttributeValueDisplayValue=${getEncodedUri(dimensionAttributeValueDisplayValue)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List EmplPostings
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of EmplPosting 
    remote isolated function listEmplPostings(map<string|string[]> headers = {}, *ListEmplPostingsQueries queries) returns EmplPostingsCollection|error {
        string resourcePath = string `/EmplPostings`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create EmplPosting
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - EmplPosting created 
    remote isolated function createEmplPostings(EmplPosting payload, map<string|string[]> headers = {}) returns EmplPosting|error {
        string resourcePath = string `/EmplPostings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get EmplPosting by key
    #
    # + dataAreaId - The company data area identifier 
    # + postingProfile - The posting profile key field 
    # + accountCode - The account code key field 
    # + num - The num key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - EmplPosting 
    remote isolated function getEmplPostings(string dataAreaId, string postingProfile, string accountCode, string num, map<string|string[]> headers = {}, *GetEmplPostingsQueries queries) returns EmplPosting|error {
        string resourcePath = string `/EmplPostings(dataAreaId=${getEncodedUri(dataAreaId)},PostingProfile=${getEncodedUri(postingProfile)},AccountCode=${getEncodedUri(accountCode)},Num=${getEncodedUri(num)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete EmplPosting
    #
    # + dataAreaId - The company data area identifier 
    # + postingProfile - The posting profile key field 
    # + accountCode - The account code key field 
    # + num - The num key field 
    # + headers - Headers to be sent with the request 
    # + return - EmplPosting deleted 
    remote isolated function deleteEmplPostings(string dataAreaId, string postingProfile, string accountCode, string num, DeleteEmplPostingsHeaders headers = {}) returns error? {
        string resourcePath = string `/EmplPostings(dataAreaId=${getEncodedUri(dataAreaId)},PostingProfile=${getEncodedUri(postingProfile)},AccountCode=${getEncodedUri(accountCode)},Num=${getEncodedUri(num)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update EmplPosting
    #
    # + dataAreaId - The company data area identifier 
    # + postingProfile - The posting profile key field 
    # + accountCode - The account code key field 
    # + num - The num key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - EmplPosting updated 
    remote isolated function updateEmplPostings(string dataAreaId, string postingProfile, string accountCode, string num, EmplPosting payload, UpdateEmplPostingsHeaders headers = {}) returns EmplPosting|error {
        string resourcePath = string `/EmplPostings(dataAreaId=${getEncodedUri(dataAreaId)},PostingProfile=${getEncodedUri(postingProfile)},AccountCode=${getEncodedUri(accountCode)},Num=${getEncodedUri(num)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ExchangeRates
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ExchangeRate 
    remote isolated function listExchangeRates(map<string|string[]> headers = {}, *ListExchangeRatesQueries queries) returns ExchangeRatesCollection|error {
        string resourcePath = string `/ExchangeRates`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ExchangeRate
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ExchangeRate created 
    remote isolated function createExchangeRates(ExchangeRate payload, map<string|string[]> headers = {}) returns ExchangeRate|error {
        string resourcePath = string `/ExchangeRates`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ExchangeRate by key
    #
    # + rateTypeName - The rate type name key field 
    # + fromCurrency - The from currency key field 
    # + toCurrency - The to currency key field 
    # + startDate - The start date key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ExchangeRate 
    remote isolated function getExchangeRates(string rateTypeName, string fromCurrency, string toCurrency, string startDate, map<string|string[]> headers = {}, *GetExchangeRatesQueries queries) returns ExchangeRate|error {
        string resourcePath = string `/ExchangeRates(RateTypeName=${getEncodedUri(rateTypeName)},FromCurrency=${getEncodedUri(fromCurrency)},ToCurrency=${getEncodedUri(toCurrency)},StartDate=${getEncodedUri(startDate)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ExchangeRate
    #
    # + rateTypeName - The rate type name key field 
    # + fromCurrency - The from currency key field 
    # + toCurrency - The to currency key field 
    # + startDate - The start date key field 
    # + headers - Headers to be sent with the request 
    # + return - ExchangeRate deleted 
    remote isolated function deleteExchangeRates(string rateTypeName, string fromCurrency, string toCurrency, string startDate, DeleteExchangeRatesHeaders headers = {}) returns error? {
        string resourcePath = string `/ExchangeRates(RateTypeName=${getEncodedUri(rateTypeName)},FromCurrency=${getEncodedUri(fromCurrency)},ToCurrency=${getEncodedUri(toCurrency)},StartDate=${getEncodedUri(startDate)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ExchangeRate
    #
    # + rateTypeName - The rate type name key field 
    # + fromCurrency - The from currency key field 
    # + toCurrency - The to currency key field 
    # + startDate - The start date key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ExchangeRate updated 
    remote isolated function updateExchangeRates(string rateTypeName, string fromCurrency, string toCurrency, string startDate, ExchangeRate payload, UpdateExchangeRatesHeaders headers = {}) returns ExchangeRate|error {
        string resourcePath = string `/ExchangeRates(RateTypeName=${getEncodedUri(rateTypeName)},FromCurrency=${getEncodedUri(fromCurrency)},ToCurrency=${getEncodedUri(toCurrency)},StartDate=${getEncodedUri(startDate)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List ExchangeRatesNonISO
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of ExchangeRateNonISO 
    remote isolated function listExchangeRatesNonISO(map<string|string[]> headers = {}, *ListExchangeRatesNonISOQueries queries) returns ExchangeRatesNonISOCollection|error {
        string resourcePath = string `/ExchangeRatesNonISO`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create ExchangeRateNonISO
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ExchangeRateNonISO created 
    remote isolated function createExchangeRatesNonISO(ExchangeRateNonISO payload, map<string|string[]> headers = {}) returns ExchangeRateNonISO|error {
        string resourcePath = string `/ExchangeRatesNonISO`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get ExchangeRateNonISO by key
    #
    # + rateTypeName - The rate type name key field 
    # + fromCurrency - The from currency key field 
    # + toCurrency - The to currency key field 
    # + startDate - The start date key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - ExchangeRateNonISO 
    remote isolated function getExchangeRatesNonISO(string rateTypeName, string fromCurrency, string toCurrency, string startDate, map<string|string[]> headers = {}, *GetExchangeRatesNonISOQueries queries) returns ExchangeRateNonISO|error {
        string resourcePath = string `/ExchangeRatesNonISO(RateTypeName=${getEncodedUri(rateTypeName)},FromCurrency=${getEncodedUri(fromCurrency)},ToCurrency=${getEncodedUri(toCurrency)},StartDate=${getEncodedUri(startDate)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete ExchangeRateNonISO
    #
    # + rateTypeName - The rate type name key field 
    # + fromCurrency - The from currency key field 
    # + toCurrency - The to currency key field 
    # + startDate - The start date key field 
    # + headers - Headers to be sent with the request 
    # + return - ExchangeRateNonISO deleted 
    remote isolated function deleteExchangeRatesNonISO(string rateTypeName, string fromCurrency, string toCurrency, string startDate, DeleteExchangeRatesNonISOHeaders headers = {}) returns error? {
        string resourcePath = string `/ExchangeRatesNonISO(RateTypeName=${getEncodedUri(rateTypeName)},FromCurrency=${getEncodedUri(fromCurrency)},ToCurrency=${getEncodedUri(toCurrency)},StartDate=${getEncodedUri(startDate)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update ExchangeRateNonISO
    #
    # + rateTypeName - The rate type name key field 
    # + fromCurrency - The from currency key field 
    # + toCurrency - The to currency key field 
    # + startDate - The start date key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - ExchangeRateNonISO updated 
    remote isolated function updateExchangeRatesNonISO(string rateTypeName, string fromCurrency, string toCurrency, string startDate, ExchangeRateNonISO payload, UpdateExchangeRatesNonISOHeaders headers = {}) returns ExchangeRateNonISO|error {
        string resourcePath = string `/ExchangeRatesNonISO(RateTypeName=${getEncodedUri(rateTypeName)},FromCurrency=${getEncodedUri(fromCurrency)},ToCurrency=${getEncodedUri(toCurrency)},StartDate=${getEncodedUri(startDate)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List LanguageCodes
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of LanguageCode 
    remote isolated function listLanguageCodes(map<string|string[]> headers = {}, *ListLanguageCodesQueries queries) returns LanguageCodesCollection|error {
        string resourcePath = string `/LanguageCodes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create LanguageCode
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - LanguageCode created 
    remote isolated function createLanguageCodes(LanguageCode payload, map<string|string[]> headers = {}) returns LanguageCode|error {
        string resourcePath = string `/LanguageCodes`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get LanguageCode by key
    #
    # + languageCodeId - The language code id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - LanguageCode 
    remote isolated function getLanguageCodes(string languageCodeId, map<string|string[]> headers = {}, *GetLanguageCodesQueries queries) returns LanguageCode|error {
        string resourcePath = string `/LanguageCodes(${getEncodedUri(languageCodeId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete LanguageCode
    #
    # + languageCodeId - The language code id key field 
    # + headers - Headers to be sent with the request 
    # + return - LanguageCode deleted 
    remote isolated function deleteLanguageCodes(string languageCodeId, DeleteLanguageCodesHeaders headers = {}) returns error? {
        string resourcePath = string `/LanguageCodes(${getEncodedUri(languageCodeId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update LanguageCode
    #
    # + languageCodeId - The language code id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - LanguageCode updated 
    remote isolated function updateLanguageCodes(string languageCodeId, LanguageCode payload, UpdateLanguageCodesHeaders headers = {}) returns LanguageCode|error {
        string resourcePath = string `/LanguageCodes(${getEncodedUri(languageCodeId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List LegalEntities
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of LegalEntity 
    remote isolated function listLegalEntities(map<string|string[]> headers = {}, *ListLegalEntitiesQueries queries) returns LegalEntitiesCollection|error {
        string resourcePath = string `/LegalEntities`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create LegalEntity
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - LegalEntity created 
    remote isolated function createLegalEntities(LegalEntity payload, map<string|string[]> headers = {}) returns LegalEntity|error {
        string resourcePath = string `/LegalEntities`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get LegalEntity by key
    #
    # + legalEntityId - The legal entity id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - LegalEntity 
    remote isolated function getLegalEntities(string legalEntityId, map<string|string[]> headers = {}, *GetLegalEntitiesQueries queries) returns LegalEntity|error {
        string resourcePath = string `/LegalEntities(${getEncodedUri(legalEntityId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete LegalEntity
    #
    # + legalEntityId - The legal entity id key field 
    # + headers - Headers to be sent with the request 
    # + return - LegalEntity deleted 
    remote isolated function deleteLegalEntities(string legalEntityId, DeleteLegalEntitiesHeaders headers = {}) returns error? {
        string resourcePath = string `/LegalEntities(${getEncodedUri(legalEntityId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update LegalEntity
    #
    # + legalEntityId - The legal entity id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - LegalEntity updated 
    remote isolated function updateLegalEntities(string legalEntityId, LegalEntity payload, UpdateLegalEntitiesHeaders headers = {}) returns LegalEntity|error {
        string resourcePath = string `/LegalEntities(${getEncodedUri(legalEntityId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List NameAffixes
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of NameAffix 
    remote isolated function listNameAffixes(map<string|string[]> headers = {}, *ListNameAffixesQueries queries) returns NameAffixesCollection|error {
        string resourcePath = string `/NameAffixes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create NameAffix
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - NameAffix created 
    remote isolated function createNameAffixes(NameAffix payload, map<string|string[]> headers = {}) returns NameAffix|error {
        string resourcePath = string `/NameAffixes`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get NameAffix by key
    #
    # + affix - The affix key field 
    # + 'type - The entity type identifier 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - NameAffix 
    remote isolated function getNameAffixes(string 'type, string affix, map<string|string[]> headers = {}, *GetNameAffixesQueries queries) returns NameAffix|error {
        string resourcePath = string `/NameAffixes(Type=${getEncodedUri('type)},Affix=${getEncodedUri(affix)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete NameAffix
    #
    # + affix - The affix key field 
    # + 'type - The entity type identifier 
    # + headers - Headers to be sent with the request 
    # + return - NameAffix deleted 
    remote isolated function deleteNameAffixes(string 'type, string affix, DeleteNameAffixesHeaders headers = {}) returns error? {
        string resourcePath = string `/NameAffixes(Type=${getEncodedUri('type)},Affix=${getEncodedUri(affix)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update NameAffix
    #
    # + affix - The affix key field 
    # + payload - The request body 
    # + 'type - The entity type identifier 
    # + headers - Headers to be sent with the request 
    # + return - NameAffix updated 
    remote isolated function updateNameAffixes(string 'type, string affix, NameAffix payload, UpdateNameAffixesHeaders headers = {}) returns NameAffix|error {
        string resourcePath = string `/NameAffixes(Type=${getEncodedUri('type)},Affix=${getEncodedUri(affix)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List NameSequences
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of NameSequence 
    remote isolated function listNameSequences(map<string|string[]> headers = {}, *ListNameSequencesQueries queries) returns NameSequencesCollection|error {
        string resourcePath = string `/NameSequences`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create NameSequence
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - NameSequence created 
    remote isolated function createNameSequences(NameSequence payload, map<string|string[]> headers = {}) returns NameSequence|error {
        string resourcePath = string `/NameSequences`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get NameSequence by key
    #
    # + nameSequence - The name sequence key field 
    # + languageId - The language id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - NameSequence 
    remote isolated function getNameSequences(string nameSequence, string languageId, map<string|string[]> headers = {}, *GetNameSequencesQueries queries) returns NameSequence|error {
        string resourcePath = string `/NameSequences(NameSequence=${getEncodedUri(nameSequence)},LanguageId=${getEncodedUri(languageId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete NameSequence
    #
    # + nameSequence - The name sequence key field 
    # + languageId - The language id key field 
    # + headers - Headers to be sent with the request 
    # + return - NameSequence deleted 
    remote isolated function deleteNameSequences(string nameSequence, string languageId, DeleteNameSequencesHeaders headers = {}) returns error? {
        string resourcePath = string `/NameSequences(NameSequence=${getEncodedUri(nameSequence)},LanguageId=${getEncodedUri(languageId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update NameSequence
    #
    # + nameSequence - The name sequence key field 
    # + languageId - The language id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - NameSequence updated 
    remote isolated function updateNameSequences(string nameSequence, string languageId, NameSequence payload, UpdateNameSequencesHeaders headers = {}) returns NameSequence|error {
        string resourcePath = string `/NameSequences(NameSequence=${getEncodedUri(nameSequence)},LanguageId=${getEncodedUri(languageId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List PaymentCalendarRules
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of PaymentCalendarRule 
    remote isolated function listPaymentCalendarRules(map<string|string[]> headers = {}, *ListPaymentCalendarRulesQueries queries) returns PaymentCalendarRulesCollection|error {
        string resourcePath = string `/PaymentCalendarRules`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create PaymentCalendarRule
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PaymentCalendarRule created 
    remote isolated function createPaymentCalendarRules(PaymentCalendarRule payload, map<string|string[]> headers = {}) returns PaymentCalendarRule|error {
        string resourcePath = string `/PaymentCalendarRules`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get PaymentCalendarRule by key
    #
    # + dataAreaId - The company data area identifier 
    # + moduleType - The module type key field 
    # + priority - The priority key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - PaymentCalendarRule 
    remote isolated function getPaymentCalendarRules(string dataAreaId, string moduleType, int priority, map<string|string[]> headers = {}, *GetPaymentCalendarRulesQueries queries) returns PaymentCalendarRule|error {
        string resourcePath = string `/PaymentCalendarRules(dataAreaId=${getEncodedUri(dataAreaId)},ModuleType=${getEncodedUri(moduleType)},Priority=${getEncodedUri(priority)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete PaymentCalendarRule
    #
    # + dataAreaId - The company data area identifier 
    # + moduleType - The module type key field 
    # + priority - The priority key field 
    # + headers - Headers to be sent with the request 
    # + return - PaymentCalendarRule deleted 
    remote isolated function deletePaymentCalendarRules(string dataAreaId, string moduleType, int priority, DeletePaymentCalendarRulesHeaders headers = {}) returns error? {
        string resourcePath = string `/PaymentCalendarRules(dataAreaId=${getEncodedUri(dataAreaId)},ModuleType=${getEncodedUri(moduleType)},Priority=${getEncodedUri(priority)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update PaymentCalendarRule
    #
    # + dataAreaId - The company data area identifier 
    # + moduleType - The module type key field 
    # + priority - The priority key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PaymentCalendarRule updated 
    remote isolated function updatePaymentCalendarRules(string dataAreaId, string moduleType, int priority, PaymentCalendarRule payload, UpdatePaymentCalendarRulesHeaders headers = {}) returns PaymentCalendarRule|error {
        string resourcePath = string `/PaymentCalendarRules(dataAreaId=${getEncodedUri(dataAreaId)},ModuleType=${getEncodedUri(moduleType)},Priority=${getEncodedUri(priority)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List PaymentDays
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of PaymentDay 
    remote isolated function listPaymentDays(map<string|string[]> headers = {}, *ListPaymentDaysQueries queries) returns PaymentDaysCollection|error {
        string resourcePath = string `/PaymentDays`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create PaymentDay
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PaymentDay created 
    remote isolated function createPaymentDays(PaymentDay payload, map<string|string[]> headers = {}) returns PaymentDay|error {
        string resourcePath = string `/PaymentDays`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get PaymentDay by key
    #
    # + dataAreaId - The company data area identifier 
    # + name - The name key field 
    # + frequency - The frequency key field 
    # + dayOfWeek - The day of week key field 
    # + dayOfMonth - The day of month key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - PaymentDay 
    remote isolated function getPaymentDays(string dataAreaId, string name, string frequency, string dayOfWeek, int dayOfMonth, map<string|string[]> headers = {}, *GetPaymentDaysQueries queries) returns PaymentDay|error {
        string resourcePath = string `/PaymentDays(dataAreaId=${getEncodedUri(dataAreaId)},Name=${getEncodedUri(name)},Frequency=${getEncodedUri(frequency)},DayOfWeek=${getEncodedUri(dayOfWeek)},DayOfMonth=${getEncodedUri(dayOfMonth)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete PaymentDay
    #
    # + dataAreaId - The company data area identifier 
    # + name - The name key field 
    # + frequency - The frequency key field 
    # + dayOfWeek - The day of week key field 
    # + dayOfMonth - The day of month key field 
    # + headers - Headers to be sent with the request 
    # + return - PaymentDay deleted 
    remote isolated function deletePaymentDays(string dataAreaId, string name, string frequency, string dayOfWeek, int dayOfMonth, DeletePaymentDaysHeaders headers = {}) returns error? {
        string resourcePath = string `/PaymentDays(dataAreaId=${getEncodedUri(dataAreaId)},Name=${getEncodedUri(name)},Frequency=${getEncodedUri(frequency)},DayOfWeek=${getEncodedUri(dayOfWeek)},DayOfMonth=${getEncodedUri(dayOfMonth)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update PaymentDay
    #
    # + dataAreaId - The company data area identifier 
    # + name - The name key field 
    # + frequency - The frequency key field 
    # + dayOfWeek - The day of week key field 
    # + dayOfMonth - The day of month key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PaymentDay updated 
    remote isolated function updatePaymentDays(string dataAreaId, string name, string frequency, string dayOfWeek, int dayOfMonth, PaymentDay payload, UpdatePaymentDaysHeaders headers = {}) returns PaymentDay|error {
        string resourcePath = string `/PaymentDays(dataAreaId=${getEncodedUri(dataAreaId)},Name=${getEncodedUri(name)},Frequency=${getEncodedUri(frequency)},DayOfWeek=${getEncodedUri(dayOfWeek)},DayOfMonth=${getEncodedUri(dayOfMonth)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List PaymentInstructions
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of PaymentInstruction 
    remote isolated function listPaymentInstructions(map<string|string[]> headers = {}, *ListPaymentInstructionsQueries queries) returns PaymentInstructionsCollection|error {
        string resourcePath = string `/PaymentInstructions`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create PaymentInstruction
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PaymentInstruction created 
    remote isolated function createPaymentInstructions(PaymentInstruction payload, map<string|string[]> headers = {}) returns PaymentInstruction|error {
        string resourcePath = string `/PaymentInstructions`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get PaymentInstruction by key
    #
    # + dataAreaId - The company data area identifier 
    # + paymentInstructionCode - The payment instruction code key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - PaymentInstruction 
    remote isolated function getPaymentInstructions(string dataAreaId, string paymentInstructionCode, map<string|string[]> headers = {}, *GetPaymentInstructionsQueries queries) returns PaymentInstruction|error {
        string resourcePath = string `/PaymentInstructions(dataAreaId=${getEncodedUri(dataAreaId)},PaymentInstructionCode=${getEncodedUri(paymentInstructionCode)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete PaymentInstruction
    #
    # + dataAreaId - The company data area identifier 
    # + paymentInstructionCode - The payment instruction code key field 
    # + headers - Headers to be sent with the request 
    # + return - PaymentInstruction deleted 
    remote isolated function deletePaymentInstructions(string dataAreaId, string paymentInstructionCode, DeletePaymentInstructionsHeaders headers = {}) returns error? {
        string resourcePath = string `/PaymentInstructions(dataAreaId=${getEncodedUri(dataAreaId)},PaymentInstructionCode=${getEncodedUri(paymentInstructionCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update PaymentInstruction
    #
    # + dataAreaId - The company data area identifier 
    # + paymentInstructionCode - The payment instruction code key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PaymentInstruction updated 
    remote isolated function updatePaymentInstructions(string dataAreaId, string paymentInstructionCode, PaymentInstruction payload, UpdatePaymentInstructionsHeaders headers = {}) returns PaymentInstruction|error {
        string resourcePath = string `/PaymentInstructions(dataAreaId=${getEncodedUri(dataAreaId)},PaymentInstructionCode=${getEncodedUri(paymentInstructionCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List PaymentMethods
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of PaymentMethod 
    remote isolated function listPaymentMethods(map<string|string[]> headers = {}, *ListPaymentMethodsQueries queries) returns PaymentMethodsCollection|error {
        string resourcePath = string `/PaymentMethods`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create PaymentMethod
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PaymentMethod created 
    remote isolated function createPaymentMethods(PaymentMethod payload, map<string|string[]> headers = {}) returns PaymentMethod|error {
        string resourcePath = string `/PaymentMethods`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get PaymentMethod by key
    #
    # + dataAreaId - The company data area identifier 
    # + payMethod - The pay method key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - PaymentMethod 
    remote isolated function getPaymentMethods(string dataAreaId, string payMethod, map<string|string[]> headers = {}, *GetPaymentMethodsQueries queries) returns PaymentMethod|error {
        string resourcePath = string `/PaymentMethods(dataAreaId=${getEncodedUri(dataAreaId)},PayMethod=${getEncodedUri(payMethod)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete PaymentMethod
    #
    # + dataAreaId - The company data area identifier 
    # + payMethod - The pay method key field 
    # + headers - Headers to be sent with the request 
    # + return - PaymentMethod deleted 
    remote isolated function deletePaymentMethods(string dataAreaId, string payMethod, DeletePaymentMethodsHeaders headers = {}) returns error? {
        string resourcePath = string `/PaymentMethods(dataAreaId=${getEncodedUri(dataAreaId)},PayMethod=${getEncodedUri(payMethod)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update PaymentMethod
    #
    # + dataAreaId - The company data area identifier 
    # + payMethod - The pay method key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PaymentMethod updated 
    remote isolated function updatePaymentMethods(string dataAreaId, string payMethod, PaymentMethod payload, UpdatePaymentMethodsHeaders headers = {}) returns PaymentMethod|error {
        string resourcePath = string `/PaymentMethods(dataAreaId=${getEncodedUri(dataAreaId)},PayMethod=${getEncodedUri(payMethod)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List PaymentTerms
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of PaymentTerm 
    remote isolated function listPaymentTerms(map<string|string[]> headers = {}, *ListPaymentTermsQueries queries) returns PaymentTermsCollection|error {
        string resourcePath = string `/PaymentTerms`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create PaymentTerm
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PaymentTerm created 
    remote isolated function createPaymentTerms(PaymentTerm payload, map<string|string[]> headers = {}) returns PaymentTerm|error {
        string resourcePath = string `/PaymentTerms`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get PaymentTerm by key
    #
    # + dataAreaId - The company data area identifier 
    # + name - The name key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - PaymentTerm 
    remote isolated function getPaymentTerms(string dataAreaId, string name, map<string|string[]> headers = {}, *GetPaymentTermsQueries queries) returns PaymentTerm|error {
        string resourcePath = string `/PaymentTerms(dataAreaId=${getEncodedUri(dataAreaId)},Name=${getEncodedUri(name)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete PaymentTerm
    #
    # + dataAreaId - The company data area identifier 
    # + name - The name key field 
    # + headers - Headers to be sent with the request 
    # + return - PaymentTerm deleted 
    remote isolated function deletePaymentTerms(string dataAreaId, string name, DeletePaymentTermsHeaders headers = {}) returns error? {
        string resourcePath = string `/PaymentTerms(dataAreaId=${getEncodedUri(dataAreaId)},Name=${getEncodedUri(name)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update PaymentTerm
    #
    # + dataAreaId - The company data area identifier 
    # + name - The name key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - PaymentTerm updated 
    remote isolated function updatePaymentTerms(string dataAreaId, string name, PaymentTerm payload, UpdatePaymentTermsHeaders headers = {}) returns PaymentTerm|error {
        string resourcePath = string `/PaymentTerms(dataAreaId=${getEncodedUri(dataAreaId)},Name=${getEncodedUri(name)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List Salutations
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of Salutation 
    remote isolated function listSalutations(map<string|string[]> headers = {}, *ListSalutationsQueries queries) returns SalutationsCollection|error {
        string resourcePath = string `/Salutations`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create Salutation
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Salutation created 
    remote isolated function createSalutations(Salutation payload, map<string|string[]> headers = {}) returns Salutation|error {
        string resourcePath = string `/Salutations`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get Salutation by key
    #
    # + dataAreaId - The company data area identifier 
    # + salutationPhrase - The salutation phrase key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Salutation 
    remote isolated function getSalutations(string dataAreaId, string salutationPhrase, map<string|string[]> headers = {}, *GetSalutationsQueries queries) returns Salutation|error {
        string resourcePath = string `/Salutations(dataAreaId=${getEncodedUri(dataAreaId)},SalutationPhrase=${getEncodedUri(salutationPhrase)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete Salutation
    #
    # + dataAreaId - The company data area identifier 
    # + salutationPhrase - The salutation phrase key field 
    # + headers - Headers to be sent with the request 
    # + return - Salutation deleted 
    remote isolated function deleteSalutations(string dataAreaId, string salutationPhrase, DeleteSalutationsHeaders headers = {}) returns error? {
        string resourcePath = string `/Salutations(dataAreaId=${getEncodedUri(dataAreaId)},SalutationPhrase=${getEncodedUri(salutationPhrase)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update Salutation
    #
    # + dataAreaId - The company data area identifier 
    # + salutationPhrase - The salutation phrase key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Salutation updated 
    remote isolated function updateSalutations(string dataAreaId, string salutationPhrase, Salutation payload, UpdateSalutationsHeaders headers = {}) returns Salutation|error {
        string resourcePath = string `/Salutations(dataAreaId=${getEncodedUri(dataAreaId)},SalutationPhrase=${getEncodedUri(salutationPhrase)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List VATNumTables
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of VATNumTable 
    remote isolated function listVATNumTables(map<string|string[]> headers = {}, *ListVATNumTablesQueries queries) returns VATNumTablesCollection|error {
        string resourcePath = string `/VATNumTables`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create VATNumTable
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VATNumTable created 
    remote isolated function createVATNumTables(VATNumTable payload, map<string|string[]> headers = {}) returns VATNumTable|error {
        string resourcePath = string `/VATNumTables`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get VATNumTable by key
    #
    # + dataAreaId - The company data area identifier 
    # + vATNum - The v at num key field 
    # + countryRegionId - The country region id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - VATNumTable 
    remote isolated function getVATNumTables(string dataAreaId, string vATNum, string countryRegionId, map<string|string[]> headers = {}, *GetVATNumTablesQueries queries) returns VATNumTable|error {
        string resourcePath = string `/VATNumTables(dataAreaId=${getEncodedUri(dataAreaId)},VATNum=${getEncodedUri(vATNum)},CountryRegionId=${getEncodedUri(countryRegionId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete VATNumTable
    #
    # + dataAreaId - The company data area identifier 
    # + vATNum - The v at num key field 
    # + countryRegionId - The country region id key field 
    # + headers - Headers to be sent with the request 
    # + return - VATNumTable deleted 
    remote isolated function deleteVATNumTables(string dataAreaId, string vATNum, string countryRegionId, DeleteVATNumTablesHeaders headers = {}) returns error? {
        string resourcePath = string `/VATNumTables(dataAreaId=${getEncodedUri(dataAreaId)},VATNum=${getEncodedUri(vATNum)},CountryRegionId=${getEncodedUri(countryRegionId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update VATNumTable
    #
    # + dataAreaId - The company data area identifier 
    # + vATNum - The v at num key field 
    # + countryRegionId - The country region id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VATNumTable updated 
    remote isolated function updateVATNumTables(string dataAreaId, string vATNum, string countryRegionId, VATNumTable payload, UpdateVATNumTablesHeaders headers = {}) returns VATNumTable|error {
        string resourcePath = string `/VATNumTables(dataAreaId=${getEncodedUri(dataAreaId)},VATNum=${getEncodedUri(vATNum)},CountryRegionId=${getEncodedUri(countryRegionId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List VRMCurrencies
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of VRMCurrency 
    remote isolated function listVRMCurrencies(map<string|string[]> headers = {}, *ListVRMCurrenciesQueries queries) returns VRMCurrenciesCollection|error {
        string resourcePath = string `/VRMCurrencies`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create VRMCurrency
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VRMCurrency created 
    remote isolated function createVRMCurrencies(VRMCurrency payload, map<string|string[]> headers = {}) returns VRMCurrency|error {
        string resourcePath = string `/VRMCurrencies`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get VRMCurrency by key
    #
    # + currencyCode - The currency code key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - VRMCurrency 
    remote isolated function getVRMCurrencies(string currencyCode, map<string|string[]> headers = {}, *GetVRMCurrenciesQueries queries) returns VRMCurrency|error {
        string resourcePath = string `/VRMCurrencies(${getEncodedUri(currencyCode)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete VRMCurrency
    #
    # + currencyCode - The currency code key field 
    # + headers - Headers to be sent with the request 
    # + return - VRMCurrency deleted 
    remote isolated function deleteVRMCurrencies(string currencyCode, DeleteVRMCurrenciesHeaders headers = {}) returns error? {
        string resourcePath = string `/VRMCurrencies(${getEncodedUri(currencyCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update VRMCurrency
    #
    # + currencyCode - The currency code key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VRMCurrency updated 
    remote isolated function updateVRMCurrencies(string currencyCode, VRMCurrency payload, UpdateVRMCurrenciesHeaders headers = {}) returns VRMCurrency|error {
        string resourcePath = string `/VRMCurrencies(${getEncodedUri(currencyCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List VRMLanguages
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of VRMLanguage 
    remote isolated function listVRMLanguages(map<string|string[]> headers = {}, *ListVRMLanguagesQueries queries) returns VRMLanguagesCollection|error {
        string resourcePath = string `/VRMLanguages`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create VRMLanguage
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VRMLanguage created 
    remote isolated function createVRMLanguages(VRMLanguage payload, map<string|string[]> headers = {}) returns VRMLanguage|error {
        string resourcePath = string `/VRMLanguages`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get VRMLanguage by key
    #
    # + languageId - The language id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - VRMLanguage 
    remote isolated function getVRMLanguages(string languageId, map<string|string[]> headers = {}, *GetVRMLanguagesQueries queries) returns VRMLanguage|error {
        string resourcePath = string `/VRMLanguages(${getEncodedUri(languageId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete VRMLanguage
    #
    # + languageId - The language id key field 
    # + headers - Headers to be sent with the request 
    # + return - VRMLanguage deleted 
    remote isolated function deleteVRMLanguages(string languageId, DeleteVRMLanguagesHeaders headers = {}) returns error? {
        string resourcePath = string `/VRMLanguages(${getEncodedUri(languageId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update VRMLanguage
    #
    # + languageId - The language id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VRMLanguage updated 
    remote isolated function updateVRMLanguages(string languageId, VRMLanguage payload, UpdateVRMLanguagesHeaders headers = {}) returns VRMLanguage|error {
        string resourcePath = string `/VRMLanguages(${getEncodedUri(languageId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List VRMParameters
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of VRMParameter 
    remote isolated function listVRMParameters(map<string|string[]> headers = {}, *ListVRMParametersQueries queries) returns VRMParametersCollection|error {
        string resourcePath = string `/VRMParameters`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create VRMParameter
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VRMParameter created 
    remote isolated function createVRMParameters(VRMParameter payload, map<string|string[]> headers = {}) returns VRMParameter|error {
        string resourcePath = string `/VRMParameters`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get VRMParameter by key
    #
    # + dataAreaId - The company data area identifier 
    # + 'key - The entity key value 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - VRMParameter 
    remote isolated function getVRMParameters(string dataAreaId, int 'key, map<string|string[]> headers = {}, *GetVRMParametersQueries queries) returns VRMParameter|error {
        string resourcePath = string `/VRMParameters(dataAreaId=${getEncodedUri(dataAreaId)},Key=${getEncodedUri('key)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete VRMParameter
    #
    # + dataAreaId - The company data area identifier 
    # + 'key - The entity key value 
    # + headers - Headers to be sent with the request 
    # + return - VRMParameter deleted 
    remote isolated function deleteVRMParameters(string dataAreaId, int 'key, DeleteVRMParametersHeaders headers = {}) returns error? {
        string resourcePath = string `/VRMParameters(dataAreaId=${getEncodedUri(dataAreaId)},Key=${getEncodedUri('key)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update VRMParameter
    #
    # + dataAreaId - The company data area identifier 
    # + payload - The request body 
    # + 'key - The entity key value 
    # + headers - Headers to be sent with the request 
    # + return - VRMParameter updated 
    remote isolated function updateVRMParameters(string dataAreaId, int 'key, VRMParameter payload, UpdateVRMParametersHeaders headers = {}) returns VRMParameter|error {
        string resourcePath = string `/VRMParameters(dataAreaId=${getEncodedUri(dataAreaId)},Key=${getEncodedUri('key)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List VRMPeople
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of VRMPerson 
    remote isolated function listVRMPeople(map<string|string[]> headers = {}, *ListVRMPeopleQueries queries) returns VRMPeopleCollection|error {
        string resourcePath = string `/VRMPeople`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create VRMPerson
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VRMPerson created 
    remote isolated function createVRMPeople(VRMPerson payload, map<string|string[]> headers = {}) returns VRMPerson|error {
        string resourcePath = string `/VRMPeople`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get VRMPerson by key
    #
    # + partyNumber - The party number key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - VRMPerson 
    remote isolated function getVRMPeople(string partyNumber, map<string|string[]> headers = {}, *GetVRMPeopleQueries queries) returns VRMPerson|error {
        string resourcePath = string `/VRMPeople(${getEncodedUri(partyNumber)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete VRMPerson
    #
    # + partyNumber - The party number key field 
    # + headers - Headers to be sent with the request 
    # + return - VRMPerson deleted 
    remote isolated function deleteVRMPeople(string partyNumber, DeleteVRMPeopleHeaders headers = {}) returns error? {
        string resourcePath = string `/VRMPeople(${getEncodedUri(partyNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update VRMPerson
    #
    # + partyNumber - The party number key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VRMPerson updated 
    remote isolated function updateVRMPeople(string partyNumber, VRMPerson payload, UpdateVRMPeopleHeaders headers = {}) returns VRMPerson|error {
        string resourcePath = string `/VRMPeople(${getEncodedUri(partyNumber)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List VRMTaxGroups
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of VRMTaxGroup 
    remote isolated function listVRMTaxGroups(map<string|string[]> headers = {}, *ListVRMTaxGroupsQueries queries) returns VRMTaxGroupsCollection|error {
        string resourcePath = string `/VRMTaxGroups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create VRMTaxGroup
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VRMTaxGroup created 
    remote isolated function createVRMTaxGroups(VRMTaxGroup payload, map<string|string[]> headers = {}) returns VRMTaxGroup|error {
        string resourcePath = string `/VRMTaxGroups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get VRMTaxGroup by key
    #
    # + dataAreaId - The company data area identifier 
    # + taxGroupCode - The tax group code key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - VRMTaxGroup 
    remote isolated function getVRMTaxGroups(string dataAreaId, string taxGroupCode, map<string|string[]> headers = {}, *GetVRMTaxGroupsQueries queries) returns VRMTaxGroup|error {
        string resourcePath = string `/VRMTaxGroups(dataAreaId=${getEncodedUri(dataAreaId)},TaxGroupCode=${getEncodedUri(taxGroupCode)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete VRMTaxGroup
    #
    # + dataAreaId - The company data area identifier 
    # + taxGroupCode - The tax group code key field 
    # + headers - Headers to be sent with the request 
    # + return - VRMTaxGroup deleted 
    remote isolated function deleteVRMTaxGroups(string dataAreaId, string taxGroupCode, DeleteVRMTaxGroupsHeaders headers = {}) returns error? {
        string resourcePath = string `/VRMTaxGroups(dataAreaId=${getEncodedUri(dataAreaId)},TaxGroupCode=${getEncodedUri(taxGroupCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update VRMTaxGroup
    #
    # + dataAreaId - The company data area identifier 
    # + taxGroupCode - The tax group code key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VRMTaxGroup updated 
    remote isolated function updateVRMTaxGroups(string dataAreaId, string taxGroupCode, VRMTaxGroup payload, UpdateVRMTaxGroupsHeaders headers = {}) returns VRMTaxGroup|error {
        string resourcePath = string `/VRMTaxGroups(dataAreaId=${getEncodedUri(dataAreaId)},TaxGroupCode=${getEncodedUri(taxGroupCode)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List VoucherTypes
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of VoucherType 
    remote isolated function listVoucherTypes(map<string|string[]> headers = {}, *ListVoucherTypesQueries queries) returns VoucherTypesCollection|error {
        string resourcePath = string `/VoucherTypes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create VoucherType
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VoucherType created 
    remote isolated function createVoucherTypes(VoucherType payload, map<string|string[]> headers = {}) returns VoucherType|error {
        string resourcePath = string `/VoucherTypes`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get VoucherType by key
    #
    # + dataAreaId - The company data area identifier 
    # + voucherType - The voucher type key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - VoucherType 
    remote isolated function getVoucherTypes(string dataAreaId, string voucherType, map<string|string[]> headers = {}, *GetVoucherTypesQueries queries) returns VoucherType|error {
        string resourcePath = string `/VoucherTypes(dataAreaId=${getEncodedUri(dataAreaId)},VoucherType=${getEncodedUri(voucherType)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete VoucherType
    #
    # + dataAreaId - The company data area identifier 
    # + voucherType - The voucher type key field 
    # + headers - Headers to be sent with the request 
    # + return - VoucherType deleted 
    remote isolated function deleteVoucherTypes(string dataAreaId, string voucherType, DeleteVoucherTypesHeaders headers = {}) returns error? {
        string resourcePath = string `/VoucherTypes(dataAreaId=${getEncodedUri(dataAreaId)},VoucherType=${getEncodedUri(voucherType)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update VoucherType
    #
    # + dataAreaId - The company data area identifier 
    # + voucherType - The voucher type key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - VoucherType updated 
    remote isolated function updateVoucherTypes(string dataAreaId, string voucherType, VoucherType payload, UpdateVoucherTypesHeaders headers = {}) returns VoucherType|error {
        string resourcePath = string `/VoucherTypes(dataAreaId=${getEncodedUri(dataAreaId)},VoucherType=${getEncodedUri(voucherType)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }

    # List Warehouses
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Collection of Warehouse 
    remote isolated function listWarehouses(map<string|string[]> headers = {}, *ListWarehousesQueries queries) returns WarehousesCollection|error {
        string resourcePath = string `/Warehouses`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create Warehouse
    #
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Warehouse created 
    remote isolated function createWarehouses(Warehouse payload, map<string|string[]> headers = {}) returns Warehouse|error {
        string resourcePath = string `/Warehouses`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get Warehouse by key
    #
    # + dataAreaId - The company data area identifier 
    # + warehouseId - The warehouse id key field 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - Warehouse 
    remote isolated function getWarehouses(string dataAreaId, string warehouseId, map<string|string[]> headers = {}, *GetWarehousesQueries queries) returns Warehouse|error {
        string resourcePath = string `/Warehouses(dataAreaId=${getEncodedUri(dataAreaId)},WarehouseId=${getEncodedUri(warehouseId)})`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Delete Warehouse
    #
    # + dataAreaId - The company data area identifier 
    # + warehouseId - The warehouse id key field 
    # + headers - Headers to be sent with the request 
    # + return - Warehouse deleted 
    remote isolated function deleteWarehouses(string dataAreaId, string warehouseId, DeleteWarehousesHeaders headers = {}) returns error? {
        string resourcePath = string `/Warehouses(dataAreaId=${getEncodedUri(dataAreaId)},WarehouseId=${getEncodedUri(warehouseId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        return self.clientEp->delete(resourcePath, headers = httpHeaders);
    }

    # Update Warehouse
    #
    # + dataAreaId - The company data area identifier 
    # + warehouseId - The warehouse id key field 
    # + payload - The request body 
    # + headers - Headers to be sent with the request 
    # + return - Warehouse updated 
    remote isolated function updateWarehouses(string dataAreaId, string warehouseId, Warehouse payload, UpdateWarehousesHeaders headers = {}) returns Warehouse|error {
        string resourcePath = string `/Warehouses(dataAreaId=${getEncodedUri(dataAreaId)},WarehouseId=${getEncodedUri(warehouseId)})`;
        map<string|string[]> httpHeaders = http:getHeaderMap(headers);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->patch(resourcePath, request, httpHeaders);
    }
}
